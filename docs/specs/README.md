# Spec 文档归档约定

- 每个独立需求使用一个新的子目录，推荐命名为 `YYYY-MM-DD-topic`。
- 同一天同主题再次立项时增加序号，例如 `YYYY-MM-DD-topic-02`，不得复用旧目录。
- 每个需求目录固定包含 `spec.md`、`plan.md`、`task.md`、`checklist.md`。
- 四份文件按审批阶段依次创建；未批准前不提前生成下一阶段文件。
- 已归档需求的四份文件不得被后续需求覆盖或改写；修订应在原目录保留明确记录，新的独立需求应新建目录。
