# 双科目作业批改 Agent MVP Spec

## 背景

当前版本要求教师手工逐题录入标准答案和评分点，配置成本较高。新流程应由教师上传试卷，并可选择是否同时上传参考答案，由后端 Agent 自动生成答案配置草稿。

没有参考答案时，Agent 先识别试卷题目并调用百炼原生联网搜索；未找到可靠答案时，再由模型自行解题并生成答案和评分点。无论答案来自参考文件、联网检索还是模型生成，都不能直接投入正式批改，必须经过教师审核确认。

系统采用单机服务器部署：试卷文件保存在服务器本地目录，任务数据、模型原始输出、检索记录、评分理由和教师最终结果保存在 SQLite；模型 API Key 仅由服务端环境变量读取。

## 目标

- 支持初中数学和高中物理两种科目。
- 从上传的试卷中自动识别题号、题目内容、题型和分值。
- 支持可选上传参考答案，并自动解析为逐题答案配置。
- 没有参考答案时优先联网搜索；无可靠结果时由模型生成。
- 记录每道题的答案来源、检索来源、模型原始结果和置信度。
- 为教师提供逐题审核、修改、通过和驳回重新生成能力。
- 只有教师审核通过的答案配置才能用于学生试卷批改。
- 保持现有教师终审、原始模型记录和班级统计流程。

## 功能需求

- F1：教师可以创建批改任务，选择“初中数学”或“高中物理”，填写任务名称、班级、试卷名称，并上传一份固定模板试卷。
- F2：教师必须选择答案配置方式：“上传参考答案”或“没有参考答案，由 Agent 查找并生成”。
- F3：选择“上传参考答案”时，支持上传 PDF、JPG、JPEG 或 PNG 格式的参考答案文件。
- F4：系统从试卷中自动识别题号、题目内容、题型和满分。初中数学支持选择题、填空题和简单简答题；高中物理支持选择题、填空题和计算题。
- F5：有参考答案时，系统结合试卷与参考答案文件，逐题提取标准答案和评分点；缺失、题号无法对应或置信度不足的题目必须明确标记。
- F6：没有参考答案时，Agent 先根据识别出的题目逐题调用百炼原生联网搜索。
- F7：联网搜索得到可靠结果时，系统保存候选答案、搜索查询、来源标题、URL、摘要和检索时间，并将答案来源标为“联网检索”。
- F8：无法检索到可归因的可靠答案时，系统调用模型独立解题，生成标准答案、评分点和简要解题依据，并将来源标为“模型生成”，不得伪装成检索结果。
- F9：每道题保存自动配置来源，包括“参考答案提取”“联网检索”“模型生成”和“教师修改”；教师修改不能覆盖原始自动结果。
- F10：系统保存试卷识别原始结果、参考答案识别结果、搜索记录、模型生成原始响应、解析结果、置信度、失败原因和时间。
- F11：答案生成完成后进入教师审核页，逐题展示原题、题型、满分、候选标准答案、评分点、来源、置信度和检索证据。
- F12：教师可以逐题修改题号、题型、满分、标准答案和评分点，并选择“审核通过”或“退回重新生成”。
- F13：教师可对失败或不满意的单题重新执行联网搜索或直接要求模型重新生成，不必重新处理整套试卷。
- F14：所有题目审核通过前，答案配置保持“待审核”，不得上传或批改学生试卷。
- F15：答案配置全部通过后锁定为当前审核版本；后续修改会使相关题目及任务重新进入待审核，并暂停使用该配置启动新批改。
- F16：高中物理计算题的评分点需要包含关键公式、代入过程、单位要求和分步得分；初中数学简单简答题需要包含关键步骤评分点。
- F17：学生试卷上传、视觉识别、逐题评分、教师改分与批注、整卷确认流程保持现有行为。
- F18：学生批改时只使用教师审核通过的标准答案和评分点，不直接使用搜索结果或未审核的模型草稿。
- F19：学生报告与班级统计仍只使用教师确认后的最终成绩。
- F20：MVP 页面保持四个主入口，但“创建任务与答案配置”页面增加科目选择、答案来源选择、自动生成进度和教师审核工作台。
- F21：API Key 仍只由服务端读取；浏览器不能获得百炼 Key、模型授权头或其他敏感配置。

## 非功能需求

- N1：继续采用单机服务器部署、SQLite 与本地文件目录，不增加账号系统和分布式队列。
- N2：试卷解析、参考答案解析、联网搜索和模型生成均采用后台任务执行，页面不能因长时间调用而冻结。
- N3：答案配置任务必须显示“等待处理、识别中、搜索中、生成中、待审核、已通过、失败”等文字状态。
- N4：服务重启后，任务文件、处理记录、答案草稿和教师审核结果必须保留；中断的处理可安全重试。
- N5：同一题重试时保留历史运行记录，不能覆盖之前的检索结果、模型响应或教师修改记录。
- N6：联网搜索只提交公开试卷题目所需的信息，不提交教师姓名、班级、学生姓名、学生答卷或其他个人信息。
- N7：检索来源 URL 和显示文本必须经过安全处理，不能在管理页面中注入脚本或任意 HTML。
- N8：参考答案与试卷上传继续校验扩展名、媒体类型、文件头、文件大小和页数，并使用系统生成的存储名称。
- N9：后台必须限制模型调用和搜索并发数；失败请求使用有限重试，避免无限调用和失控计费。
- N10：自动识别结果必须经过结构校验，包括题号唯一、题型受支持、满分合法、评分点总分不超过满分，以及每道题均有答案来源。
- N11：低置信度、题目缺失、参考答案无法对应、来源不可访问或不同来源结果冲突时，必须明确提示教师，不得自动审核通过。
- N12：只有教师审核通过的配置版本可以进入学生批改；该约束必须由服务端执行，不能只依赖前端按钮禁用。
- N13：模型 API Key 只存在于服务端环境变量和内存配置中，不写入任务记录、原始请求快照、日志或浏览器响应。
- N14：桌面端优先适配；教师在不离开审核页的情况下能够查看原题、答案候选、来源证据并完成修改确认。
- N15：自动配置过程中的每个关键操作均写入审计记录，包括上传、识别、检索、模型生成、教师修改、审核通过、退回和重新生成。

## 不做的事情

- 不支持初中数学和高中物理之外的科目。
- 初中数学不支持作图题、复杂证明题和开放性探究题。
- 高中物理不支持实验设计题、作图题及复杂综合题。
- 不支持 Word、Excel、压缩包和网页链接作为试卷或参考答案输入。
- 不建设通用搜索引擎或网页爬虫；联网检索仅使用百炼原生搜索能力。
- 不抓取登录后内容、付费内容或需要绕过访问限制的答案资源。
- 不保证互联网中的答案一定正确；检索结果和模型生成结果都必须由教师审核。
- 不允许自动生成的答案配置绕过教师审核直接用于学生批改。
- 不对学生答卷进行联网搜索，搜索阶段只处理固定模板中的公开题目内容。
- 不自动把参考答案文件中无法对应的内容强行匹配到题目。
- 不建设校本题库、向量知识库或跨任务答案复用系统。
- 不支持多名教师同时编辑同一答案配置，也不实现版本合并。
- 不自动发布答案或成绩给学生、家长及第三方平台。
- 不承诺从任意版式试卷中准确切题；首版仍限定固定模板和清晰扫描件。

## 验收标准

- AC1：创建任务时可选择“初中数学”或“高中物理”，并且只显示该科目支持的题型。
- AC2：教师上传固定模板试卷并选择“上传参考答案”后，系统能从两份文件中生成逐题答案配置草稿。
- AC3：教师选择“没有参考答案”后，系统先识别题目，再逐题执行百炼原生联网搜索。
- AC4：检索到可靠答案时，审核页显示答案、评分点、来源标题、URL、摘要和检索时间，来源明确标为“联网检索”。
- AC5：无法检索到可靠答案时，系统自动调用模型解题，来源明确标为“模型生成”，并保存生成依据和原始响应。
- AC6：参考答案缺题、题号无法对应、识别置信度低或来源结果冲突时，对应题目显示明确异常，不会自动审核通过。
- AC7：初中数学简单简答题能生成关键步骤评分点；高中物理计算题能生成关键公式、代入过程、单位要求和分步分值。
- AC8：教师可以修改题号、题型、满分、标准答案和评分点，修改后仍可查看最初的参考答案、搜索或模型生成结果。
- AC9：教师可以逐题审核通过、退回、重新搜索或重新生成，不需要重新处理整套试卷。
- AC10：所有题目审核通过前，服务端拒绝上传或批改学生试卷；不能仅通过绕过前端按钮解除限制。
- AC11：答案配置审核通过后可用于学生批改；后续修改任一题会使配置重新进入待审核，并阻止启动新的批改任务。
- AC12：刷新页面或重启服务后，试卷、参考答案、题目识别结果、搜索来源、模型原始响应、答案草稿和教师审核状态仍然存在。
- AC13：后台处理过程中页面显示识别、搜索、生成、待审核、已通过和失败状态；单题失败可以重试。
- AC14：联网搜索请求不包含教师、班级、学生或学生答卷信息；浏览器和 SQLite 中不存在百炼 API Key。
- AC15：答案配置完成后，现有学生试卷上传、自动初评、教师改分批注、整卷确认、学生报告和班级统计流程仍能完整运行。
- AC16：学生批改只使用教师审核通过的答案版本；学生报告与班级统计仍只使用教师确认后的最终成绩。
- AC17：系统为上传、识别、检索、模型生成、教师修改、审核、退回和重新处理保存可追溯记录。
- AC18：使用真实百炼配置时，“有参考答案”和“无参考答案”两条完整流程均能成功执行至少一套受控试卷。

## 已确认约束

- 科目：初中数学与高中物理。
- 初中数学题型：选择题、填空题和简单简答题。
- 高中物理题型：选择题、填空题和计算题。
- 模板：单个任务使用固定试卷模板。
- 答案来源：参考答案提取；或百炼原生联网搜索，失败后模型生成。
- 审核：自动配置与学生成绩均必须由教师确认。
- 模型：阿里云通义千问视觉模型，真实调用。
- 部署：单机服务器前后端一体。
- 存储：本地文件目录加 SQLite。
- 安全：模型 API Key 仅通过服务端环境变量提供。

---

# Python 后端迁移与试卷结构容错解析增量 Spec

## 背景

当前系统由 React 前端、Express/TypeScript 后端、SQLite 和本地文件目录组成。教师已确认本次仅把后端迁移到 Python，React 页面及其现有交互保持不变，已有 SQLite 数据、上传文件和答案版本记录必须继续可用。

真实百炼运行记录表明，本次 `ANSWER_EXTRACTION_INVALID` 并非模型返回了非法 JSON：模型成功识别出整卷题目，但第 5–8 题的评分点合计超过对应题目满分。当前解析器采用整卷一次性严格校验，因此少数可修正字段导致所有已正确识别题目一并失败，并只向教师显示“模型返回的试卷结构无法解析”。

## 目标

- 将现有服务端能力迁移到 Python 后端，继续向 React 前端提供兼容的 HTTP API、静态资源入口和错误响应结构。
- 原位复用现有 SQLite 数据库与上传目录，不要求教师重新创建任务、重新上传试卷或丢弃历史模型与审核记录。
- 对百炼返回执行分层解析：提取 JSON、识别结构、逐题校验、可恢复字段归一化和不可恢复错误隔离，避免单题偏差导致整卷失败。
- 在“没有参考答案”模式的试卷结构识别阶段忽略模型误生成的答案和评分点，仅保留题目结构，随后仍按“联网搜索优先、模型生成回退”配置答案。
- 对评分点超过满分等可恢复问题进行受控归一化，同时标记“需教师关注”并保留原始输出和归一化理由，不静默修改证据。
- 当响应确实无法解析时，向教师显示可操作的失败摘要和具体字段路径，并允许安全重试；原始百炼响应继续只读留档。
- 保持初中数学、高中物理、答案审核、版本准入、学生批改、教师终审、报告统计和安全边界不回退。

## 功能需求

- PF1：Python 后端必须覆盖现有任务、文件、答案配置、联网检索、模型生成、学生提交、自动批改、教师复核、报告统计和模型状态接口；React 前端不需要因后端迁移改变现有操作流程。
- PF2：Python 后端必须读取并继续写入现有 SQLite 数据库和上传目录，保留任务 ID、文件记录、答案版本、学生提交、模型原始响应、教师修改、审计记录及其关联关系。
- PF3：单机生产进程必须同时提供 `/api` 接口和已经构建的 React 静态页面；前端路由刷新后仍返回应用页面。
- PF4：试卷及参考答案继续支持 PDF、JPG、JPEG 和 PNG；PDF 转图必须正确处理多页文件并在成功、失败或服务中断时释放文件及渲染资源。
- PF5：真实百炼视觉识别、联网搜索和答案生成继续由服务端发起，模型名称、Base URL、超时、并发和 API Key 继续从服务端配置读取。
- PF6：试卷结构解析必须能从纯 JSON、Markdown JSON 代码块、JSON 前后附带说明文字、根对象或题目数组中提取候选结构；无法确定边界时不得执行不受控的字符串求值。
- PF7：解析器必须接受不改变语义的常见字段差异，包括数值型或字符串型题号、数字字符串分值、布尔字符串、英文或约定中文题型名称，以及百炼兼容接口返回的文本内容形态。
- PF8：每道题必须独立执行字段归一化和校验。某一道题存在可恢复偏差时，其他合法题目仍进入教师审核，不得整卷失败。
- PF9：在“没有参考答案，由 Agent 查找并生成”模式下，结构识别阶段只采纳题号、题干、题型和满分；模型提前返回的标准答案和评分点必须从识别草稿中清除，之后仍逐题执行联网搜索和模型回退。
- PF10：在“上传参考答案”模式下，系统采纳模型提取的答案和评分点。评分点出现负值、非数字或合计超过满分时，系统必须丢弃非法项或按题目满分等比例归一化合法项，并把该题标记为“需教师关注”，显示原值、调整结果和原因。
- PF11：缺失题号、重复题号、未知题型、数字字符串、缺省置信度等可恢复问题必须生成明确的待核字段或安全默认值，并阻止该题自动审核通过；教师可以在答案审核页修正。
- PF12：缺少题干、满分无法转换为正数或题目节点完全不是对象等不可恢复问题必须作为独立解析问题记录。只要仍有可用题目，任务进入教师审核并显示遗漏数量、位置和原因。
- PF13：首次响应没有任何可用题目时，系统最多追加一次“结构修复”模型请求；修复请求只发送原始模型文本和目标结构，不重复发送试卷、参考答案或个人信息，并单独保存调用记录和 Token 用量。
- PF14：本地解析和一次结构修复均失败后，任务进入可重试失败状态。教师可看到错误阶段、字段路径、问题摘要和运行 ID，但浏览器不能获得调用栈、服务器绝对路径、授权头或 API Key。
- PF15：系统必须分别保存模型原始响应、原始模型文本、局部解析问题、归一化后的逐题结果、归一化理由和最终教师结果；后续重试不能覆盖以前的运行记录。
- PF16：教师可以对整卷识别失败重新启动配置，也可以对已生成的单题继续使用重新搜索、重新生成、修改、退回、通过和发布操作。
- PF17：答案配置发布准入保持不变：存在待核字段、解析遗漏、失败题或未审核题时不能发布；只有全部题目经教师确认后才能上传和批改新的学生试卷。
- PF18：学生批改继续绑定已发布答案版本；Python 后端上线前创建的学生报告和班级统计必须保持相同答案版本、教师最终分和确认状态。
- PF19：Python 后端必须继续记录任务上传、试卷识别、结构修复、逐题归一化、检索、生成、教师修改、审核、发布、批改和整卷确认审计事件。
- PF20：服务启动时必须识别并恢复中断中的答案配置和批改运行，把它们转换为明确的可重试状态，不能永久停留在处理中。

## 非功能需求

- PN1：继续采用单机部署、SQLite 和本地文件目录，不引入 Redis、外部消息队列、对象存储或额外数据库。
- PN2：迁移后的 API 路径、HTTP 方法、主要状态码、成功响应 envelope 和错误响应 envelope 必须与现有 React 客户端兼容；确需新增字段时只能采用向后兼容的可选字段。
- PN3：现有 SQLite 数据迁移必须幂等并在事务中执行。迁移失败时完整回滚、停止启动并给出备份恢复提示，不能让服务使用部分迁移后的数据库。
- PN4：服务必须能在当前 Windows 单机服务器环境运行，路径处理不能依赖 Unix 分隔符、大小写规则或仅 Linux 可用的系统命令。
- PN5：PDF 和图片转换必须设置文件大小与页数上限，按页释放渲染对象，避免把多份完整试卷的未压缩位图长期保存在内存中。
- PN6：模型识别、结构修复、联网搜索、答案生成和学生批改必须在受控后台任务中运行；普通任务查询、进度查询和页面静态资源不能因模型请求而长时间阻塞。
- PN7：视觉识别、联网搜索和生成分别设置有限并发、请求超时和有限重试。持续限流、网络错误或服务错误不得形成无限请求或失控计费。
- PN8：结构归一化必须是确定性本地处理，禁止执行模型返回的代码、表达式或任意对象构造；JSON 恢复只能扫描和解析数据边界。
- PN9：API Key、授权头和完整环境变量不能进入 SQLite、请求快照、浏览器响应、前端构建产物或普通日志。
- PN10：日志必须包含任务 ID、答案版本 ID、运行 ID、阶段和可操作错误码；默认不记录整份试卷 Base64、完整学生答卷或未经截断的模型响应。
- PN11：文件下载与预览只能访问数据根目录内已登记的文件；上传继续校验扩展名、媒体类型、文件签名、大小和页数，搜索来源继续只允许 HTTP(S)。
- PN12：答案配置启动、结构修复和逐题重试必须防止同一目标并发重复执行；服务重启后的恢复操作必须可重复运行而不产生重复正式题目或重复发布版本。
- PN13：数据库时间、模型运行时间、批准时间和审计时间继续使用可排序的统一时间格式；Python 与旧后端写入的数据必须能够相互解释。
- PN14：模型原始响应和解析诊断允许较大，但接口默认只返回教师审核所需内容；查看单次原始运行时按运行 ID 单独读取，避免任务列表携带全部大字段。
- PN15：非模型 API 在本机正常负载下应保持交互可用；批量任务数量和模型并发继续受环境配置限制，不能按上传数量无限创建并行工作。
- PN16：服务关闭时必须停止接收新后台任务，等待或明确中止正在运行的任务，并依照恢复规则持久化可重试状态。
- PN17：自动化测试默认使用假模型和隔离临时数据库，不发送付费百炼请求；真实百炼验收必须显式运行并提醒可能产生费用。
- PN18：Python 依赖必须锁定可复现版本；安装、开发启动、生产启动、数据库备份恢复和从旧后端切换的步骤必须写入 README。
- PN19：迁移完成前保留旧数据备份；验收通过后由 Python 后端成为唯一写入者，禁止新旧两个后端同时连接同一 SQLite 数据库执行写操作。
- PN20：所有解析容错、评分点归一化、局部失败隔离、结构修复上限、旧数据库兼容和敏感信息保护都必须有自动化测试。

## 不做的事

- 不把 React 前端改写为 Python 模板、桌面 GUI 或其他前端框架。
- 不改变四个 MVP 主入口及教师现有的任务创建、答案审核、学生复核和报告统计工作流。
- 不把 SQLite 更换为 PostgreSQL、MySQL 或云数据库，也不把本地文件迁移到对象存储。
- 不支持多个 Python 服务进程同时写同一个 SQLite 数据库，不建设分布式任务队列或水平扩容方案。
- 不保留 Express 后端作为长期双写、热备或兼容服务；切换完成后只允许 Python 后端写入正式数据。
- 不自动重算、覆盖或重新绑定迁移前已经发布的答案版本、学生批改结果和教师最终成绩。
- 不因模型输出偏差而猜测缺失的完整题干、题目满分或唯一正确题号；无法可靠恢复的字段必须留给教师处理。
- 不允许结构修复请求循环调用；每次整卷识别最多追加一次结构修复。
- 不把模型提前生成的答案当作“联网检索”结果，也不在无参考答案的结构识别阶段直接采用其评分点。
- 不允许评分点归一化、题型映射或默认值填充后自动审核通过；发生自动修正的题目必须由教师确认。
- 不让解析容错跳过答案配置发布准入、学生成绩教师终审或任何已有服务端权限边界。
- 不对学生答卷执行联网搜索，联网检索仍只处理固定模板中的公开题目。
- 不扩展初中数学、高中物理之外的科目，也不新增当前范围外的作图、实验设计、复杂证明或开放探究题型。
- 不新增教师账号、角色权限、多租户、多校区、多教师协同编辑或答案版本合并。
- 不默认执行会产生费用的真实百炼测试；真机验收继续由教师使用脱敏材料显式启动。
- 不保证任意损坏、加密、超页数或极低清晰度文件都能成功识别；这类文件必须返回明确、可操作的上传或处理错误。

## 验收标准

- PAC1：在 Windows 单机上启动 Python 生产服务后，健康接口、任务 API、模型状态接口和 React 页面均返回成功；直接刷新前端子路由仍能进入对应页面。
- PAC2：复制现有正式 SQLite 与上传目录后启动 Python 服务，迁移前后的任务、文件、答案版本、正式题目、学生提交、模型运行、教师复核、报告和审计记录数量一致，原 ID 与关联关系不变。
- PAC3：现有 React 前端无需修改 API 调用方式即可完成任务列表、任务创建、答案审核、学生上传、逐题复核、学生报告和班级统计。
- PAC4：上传本次 7 页高中物理 PDF 后，Python 文件处理器输出连续的第 1–7 页模型图像，中文、公式和示意图可读，任务不会因 PDF 资源清理失败。
- PAC5：把本次真实百炼响应作为固定夹具输入无参考答案流程时，系统识别出全部可用题目，清除模型提前生成的标准答案和评分点，并继续进入逐题搜索；第 5–8 题不再导致整卷 `ANSWER_EXTRACTION_INVALID`。
- PAC6：在参考答案流程中构造评分点合计超过满分的计算题，系统把合法评分点归一化到不超过满分，标记“需教师关注”，并同时展示调整前数值、调整后数值和原因。
- PAC7：模型分别返回纯 JSON、Markdown 代码块、带前后说明的 JSON、题目数组根节点、字符串数字、布尔字符串和约定题型别名时，系统均能得到符合统一契约的题目草稿。
- PAC8：一份响应同时包含合法题目、可恢复题目和不可恢复节点时，合法题目保持原值，可恢复题目带明确待核标记，不可恢复节点形成带位置的解析问题；任务进入教师审核而不是整卷失败。
- PAC9：响应完全没有可用题目时只发起一次结构修复调用。修复成功后进入审核；修复再次失败后任务进入可重试失败，运行历史中恰好保留原识别和一次修复两条记录。
- PAC10：解析失败页面和 API 响应显示运行 ID、失败阶段、错误码、字段路径及截断摘要，不包含服务器绝对路径、调用栈、授权头或 API Key。
- PAC11：教师查看一次识别运行时，可以区分模型原始文本、局部解析问题、归一化结果和调整理由；教师后续修改不会覆盖这些原始记录。
- PAC12：存在待核字段、解析遗漏、失败题或未审核题时，前端发布按钮不可用且直接调用发布 API 仍被拒绝；全部修正并逐题通过后可以发布。
- PAC13：教师可以重新启动整卷失败任务；对已经生成的题目可以单独重新搜索或重新生成，且不会新增重复正式题目或覆盖历史运行。
- PAC14：初中数学的选择、填空、简单简答题和高中物理的选择、填空、计算题均能完成答案配置、发布、学生初评、教师改分批注和整卷确认。
- PAC15：用 Python 服务打开迁移前已经确认的学生报告和班级统计，答案版本、逐题最终分、总分、教师批注、确认人及统计结果与迁移前一致。
- PAC16：在试卷识别、结构修复、联网搜索、模型生成和学生批改阶段分别模拟服务中断，重启后相关运行均变为明确的可重试状态，不永久停留在处理中。
- PAC17：模型状态、API 响应、请求快照、SQLite、日志和前端构建产物中均搜索不到测试 API Key；文件预览不能访问数据根目录之外的路径。
- PAC18：持续返回 429 或可恢复服务错误时只执行配置上限内的有限重试；并发夹具记录到的峰值不超过答案配置和学生批改各自的配置值。
- PAC19：迁移脚本连续执行两次不改变数据；注入迁移失败后数据库完整回滚且服务拒绝以部分新模式启动。
- PAC20：开发启动、生产构建与启动、数据库备份恢复、旧后端停机、Python 后端切换及回滚步骤均可按 README 在新环境复现。
- PAC21：Python 后端单元和集成测试、React 类型检查和 UI 测试全部通过，测试退出后没有遗留服务器、数据库连接、后台任务或计时器。
- PAC22：使用脱敏试卷显式执行真实百炼验收时，有参考答案和无参考答案两条流程各成功完成一套；保存模型 ID、运行 ID、Token 用量、来源、归一化记录和教师审核结果。
