# 试卷题目识别与参考答案匹配 Spec

## 背景

当前仓库仅保留了一批高中物理试卷样本，旧实现已经从工作树中移除，本次按更小范围重新建设第一阶段能力。样本同时包含 PDF、DOCX，以及“原卷版/解析版”和“试卷/答案”两类配对方式。试卷中存在公式、上下标、选项、示意图和跨页题目，仅依赖文档文本层会丢失关键信息。

第一阶段只解决教师准备标准题库前的基础工作：识别试卷题目、识别教师上传的参考答案，并把答案可靠地匹配到对应题目。匹配结果必须可追溯、可人工修正，不能把不确定结果伪装成正确匹配。

## 目标

- 提供一个可在本机浏览器直接操作的网页。
- 支持分别上传试卷和参考答案，格式包括 PDF、DOCX、JPG、PNG。
- 使用阿里云百炼视觉模型分别识别题目与答案条目。
- 通过“题号优先、题干相似度辅助、文档顺序兜底”的确定性流程进行一对一匹配。
- 展示每道题的识别结果、答案、解析、匹配依据、置信度和异常提示。
- 允许教师修正题目信息、答案内容及匹配关系，并确认最终结果。
- 保存原文件、模型原始结果、自动匹配结果和人工修改结果，刷新或重启后可继续处理。

## 功能需求

- F1：教师可以在本地网页创建一个识别任务，并分别选择一份试卷文件和一份参考答案文件。
- F2：试卷和答案均支持 PDF、DOCX、JPG、PNG；系统必须校验扩展名、实际文件类型、文件大小、页数和空文件。
- F3：系统必须安全保存上传文件及其元数据，文件名冲突不得覆盖已有文件。
- F4：系统必须把不同格式统一转换为有序页面图像，保留公式、上下标、选项和示意图的视觉信息；每个页面保留原始页码。
- F5：系统必须从试卷中识别题号、完整题干、选项、题型、分值、来源页码、识别置信度和异常提示。
- F6：系统必须从答案文件中识别题号提示、题干提示（若存在）、标准答案、解析、来源页码、识别置信度和异常提示。
- F7：答案识别必须同时兼容“只有题号与答案/解析”的答案文件，以及“重复完整题干并附答案/解析”的解析版文件。
- F8：试卷识别和答案识别必须作为两个独立阶段执行并保存结果；任一阶段失败时都应显示失败阶段和可理解的错误。
- F9：系统必须自动执行一对一匹配。唯一规范化题号一致时优先匹配；题号缺失或冲突时可使用题干和选项相似度；顺序只能作为辅助信号。
- F10：重复题号、多条答案竞争同一道题、一道答案竞争多道题、题干相似度不足或候选差距过小时，不得强行确认，必须标记为待人工处理。
- F11：系统必须为每个匹配保存匹配方式、各项匹配信号、综合置信度和异常原因；未匹配题目和未使用答案条目都必须单独展示。
- F12：处理期间网页必须展示上传、页面转换、题目识别、答案识别、匹配、待确认、已完成和失败状态，并可刷新查看进度。
- F13：审核工作台必须逐题展示题目信息、建议答案、解析、来源页码、匹配依据和置信度，并提供原试卷与答案文件预览。
- F14：教师可以修改题号、题干、选项、题型、分值、标准答案和解析，也可以为题目重新选择答案条目、解除匹配或直接填写答案。
- F15：教师可以逐题确认。只要存在未匹配题目、匹配冲突、必填字段缺失或未确认题目，任务就不能标记为完成。
- F16：教师可以重新执行失败阶段或重新运行整套识别；重试必须保留之前的运行和原始结果，不得静默覆盖历史。
- F17：系统必须保存模型名称、提示词版本、调用阶段、请求摘要、原始响应、解析问题、Token 用量和时间；API Key 不得进入浏览器、数据库、日志或响应快照。
- F18：最近创建的任务必须可重新打开；服务重启后处于处理中的任务必须转成明确的可重试状态，不能永久卡在处理中。

## 非功能需求

- N1：首版面向 Windows 单机运行，前后端通过一个启动入口提供服务。
- N2：默认单文件大小上限为 30 MB、单文件页数上限为 30 页，均可通过服务端配置调整。
- N3：同一任务最多同时执行一个识别流水线，避免重复点击造成并发写入和重复模型调用。
- N4：模型请求默认超时 120 秒，针对限流和服务端临时错误最多进行有限次数指数退避重试。
- N5：本地解析、匹配和保存必须具备确定性；使用同一组已识别条目重复匹配应得到相同结果。
- N6：任何自动匹配都不得删除原始识别条目；人工修改必须与自动结果分层保存。
- N7：所有文件访问必须限制在配置的数据目录内，预览接口不得读取任意本地路径。
- N8：错误响应不得包含调用栈、服务器绝对路径、授权头、API Key 或完整模型请求中的文件数据。
- N9：主要页面在 1280×720 及以上桌面窗口可完整使用；处理状态、低置信度和冲突不能只依靠颜色表达。
- N10：核心解析、匹配、状态流转和 API 必须有自动化测试；至少使用仓库中的一组真实试卷/答案样本完成端到端验收。

## 不做的事情

- 不上传或识别学生答卷。
- 不进行自动批改、评分点生成、得分计算、教师评语或班级统计。
- 不联网搜索答案，也不让模型在缺少参考答案时自行解题。
- 不提供多用户账号、权限、云端同步或公网部署能力。
- 不自动发布为不可变答案版本；本阶段以教师确认后的本地匹配结果为终点。
- 不支持加密 PDF、损坏文件、扫描压缩包、ZIP 内批量文件、HEIC、TIFF、PPT 或 XLSX。
- 不承诺识别手写答案；首版输入以印刷体试卷和参考答案为准。
- 不实现面向专业排版的完整 LaTeX 环境；公式编辑仅覆盖审核工作所需的常用高中数学和物理结构。

## 验收标准

- AC1：上传一份受支持格式的试卷和答案后，系统创建任务、保存两份文件并进入处理流程。（F1-F3）
- AC2：分别上传 PDF、DOCX、JPG、PNG 样本时，系统生成顺序正确、页码连续且视觉可读的页面；不支持或超限文件得到明确错误。（F2-F4）
- AC3：对仓库中的物理试卷样本运行识别后，每道可用题目包含题号、题干、选项、题型、分值或明确的缺失提示，并能定位到来源页。（F5）
- AC4：上传答案精简版时，系统识别题号、答案和解析；上传解析版时，还能识别题干提示，并且两种文件都进入同一答案条目结构。（F6-F7）
- AC5：处理进度按试卷识别、答案识别和匹配阶段更新；任一阶段失败时显示错误码、简要原因和重试入口。（F8、F12、F16）
- AC6：唯一题号一致的题目与答案自动匹配，匹配记录显示题号命中依据；重复执行匹配结果一致。（F9、N5）
- AC7：题号缺失但题干高度相似且候选唯一时可自动建议匹配，并显示题干相似度和顺序信号。（F9、F11）
- AC8：重复题号、竞争候选或低相似度场景不会被自动确认，相关题目和答案均进入待人工处理区。（F10-F11）
- AC9：审核页能查看原卷和答案预览，并逐题对照识别字段、答案、解析、来源页、匹配依据及置信度。（F13）
- AC10：教师修改题目或答案、重新指定或解除匹配后刷新页面，修改仍然存在，同时仍可查看原始自动结果。（F14、N6）
- AC11：存在未匹配、冲突、必填字段缺失或未确认题目时，前端完成按钮不可用，直接调用完成接口也被服务端拒绝；全部修正并确认后可完成任务。（F15）
- AC12：重新运行识别后，旧运行及其模型原始响应仍可查询，新结果使用新的运行标识。（F16-F17）
- AC13：浏览器网络响应、SQLite、应用日志和前端构建产物中均不存在测试 API Key；预览接口不能访问数据根目录外文件。（F17、N7-N8）
- AC14：处理过程中重启服务后，任务变为可重试状态；最近任务列表、原文件和已保存审核修改仍可访问。（F18）
- AC15：自动化测试覆盖文件校验、结构解析、题号规范化、匹配冲突、人工覆盖、完成准入和重启恢复，并全部通过。（N10）
- AC16：使用仓库中的一组“原卷版＋解析版”和一组“试卷＋答案”样本完成两条端到端流程，所有题目均被匹配或明确列为待人工处理，不存在静默丢题或答案重复占用。（F7-F15、N10）

## 已确认约束

- 第一版同时支持 PDF、DOCX、JPG、PNG。
- 交付形式为可直接操作的本地网页。
- 视觉识别使用阿里云百炼通义千问视觉模型，API Key 仅由服务端读取。
- 答案文件同时兼容答案精简版和包含完整题目的解析版。
- 第一阶段结果字段包括题号、题干、选项、题型、分值、标准答案、解析、匹配置信度和异常提示。
- 第一阶段不做学生答卷批改和评分点生成。
- 采用“分别识别、确定性匹配、人工兜底”的方案，不采用把两份文件一次性交给模型直接配对的方案。

## 增量需求：公式显示与无门槛编辑

### 背景

当前审核页把题干、选项、标准答案和解析中的公式源码直接显示在普通文本框内，例如 `$F=k\frac{q_1q_2}{r^2}$`。教师看到的是 LaTeX 标记而不是排版后的公式，阅读困难；当识别结果需要修正时，要求教师直接修改 LaTeX 也存在明显使用门槛。

### 目标

- 审核页中的行内公式和独立公式默认以规范的数学排版显示，不再把 LaTeX 标记直接暴露给教师。
- 普通文字仍可直接输入和修改；点击公式时提供可视化编辑能力，教师可通过键盘和符号工具栏修改公式，无需学习 LaTeX。
- 系统内部仍保存可追溯、可再次渲染的公式文本，不改变现有题目、答案和解析的数据接口与保存流程。
- 公式格式异常时页面不得崩溃，应显示原始内容并提示该公式需要人工检查。

### 功能需求

- F19：审核页必须对题干、选项、标准答案和解析中的数学内容进行公式渲染，支持行内公式与独立成行公式。
- F20：系统必须兼容识别结果中常见的 `$...$`、`$$...$$`、`\(...\)`、`\[...\]` 数学分隔符；普通文字、中文标点和换行必须保持原有顺序。
- F21：各内容区域默认显示排版后的“阅读视图”。教师点击“编辑”后进入混合编辑状态，普通文字可以直接输入，已有公式以可选择的公式对象呈现，不要求教师查看或输入 LaTeX 源码。
- F22：公式编辑器必须提供常用高中数学和物理符号及结构，包括分数、根号、上下标、括号、绝对值、希腊字母、求和、积分、极限、矢量、箭头、乘除号、不等号和常用单位书写；同时支持键盘输入数字、英文字母和运算符。
- F23：教师必须能够在文字中插入新公式、选择并修改已有公式、删除公式，并在保存前看到最终排版效果；取消编辑时恢复到本次编辑前的内容。
- F24：保存时系统必须把文字与公式转换为现有接口可接受的字符串，刷新页面后公式结构、文字顺序和换行保持一致，不得因重复编辑产生额外转义或公式损坏。
- F25：无法解析或不完整的公式必须就地显示原始内容和“公式需检查”提示；教师可以删除该片段或通过可视化编辑器重新录入，单个公式错误不得阻断整道题的显示与保存。
- F26：已关联答案条目导致标准答案或解析不可编辑时，公式区域仍必须正确渲染；其编辑权限继续遵循现有的答案关联规则。

### 非功能需求

- N11：公式渲染与编辑所需的脚本、样式和字体必须随应用本地提供，运行时不得依赖公共 CDN 或外部网络。
- N12：识别结果必须作为纯文本和受控公式语法处理，不得把模型返回内容直接作为 HTML 注入页面；公式内容不得执行脚本、加载远程资源或突破浏览器安全边界。
- N13：单道题的题干、选项、答案和解析切换到阅读视图后应立即可用；正常长度内容的输入和公式编辑不得出现明显卡顿，公式区域更新不得触发整页不必要的重复渲染。
- N14：公式显示和编辑必须兼容项目当前支持的桌面版 Chrome 与 Edge，并在 1280×720 及以上窗口内保持可滚动、操作按钮可达，不得再次遮挡底部确认操作。
- N15：公式字号、行高和颜色必须与现有中文正文协调；较长的独立公式允许横向滚动或合理换行，不得撑破审核栏或覆盖相邻内容。
- N16：阅读视图、编辑入口、公式工具栏和错误提示必须支持键盘访问，并提供可被辅助技术识别的名称；公式状态不能只依靠颜色表达。
- N17：新增公式解析、序列化、异常降级和交互行为必须有自动化测试；前端构建、类型检查及现有测试不得回归。

### 本次不做

- 不让教师直接编辑 LaTeX 源码，也不提供任意宏、宏包、自定义命令或完整 LaTeX 文档排版能力。
- 不在本次改动中重新识别原文件、自动推断缺失公式或纠正模型识别出的数学含义；识别错误由教师依据原页人工修正。
- 不实现手写公式识别、手写笔输入、几何作图、函数绘图、化学结构式或电路图编辑器。
- 不改变后端题目与答案的数据结构、匹配算法、确认流程及历史任务数据。
- 不为手机端设计完整的公式编辑体验；首版仍以桌面版 Chrome 和 Edge 为目标。

### 增量验收标准

- AC17：打开包含 `$F=k\frac{q_1q_2}{r^2}$`、`\(E=\frac{F}{q}\)` 和独立公式的历史审核任务时，题干、选项、标准答案及解析均显示为排版后的公式，页面不暴露数学分隔符和命令文本。（F19-F20）
- AC18：中文、公式、标点和多行内容混排时，显示顺序与已保存字符串一致；分数、根号、上下标、希腊字母、矢量和积分等常用结构可正确显示。（F20、F22）
- AC19：教师不输入 LaTeX，即可进入编辑状态，在普通文字中插入公式，并通过键盘和工具栏完成 `F=kq₁q₂/r²` 等公式的录入、修改和删除；保存前能够看到最终排版效果。（F21-F23）
- AC20：保存混合内容并刷新页面后，文字、公式、标点及换行保持不变；连续进行至少三次“编辑—保存—刷新”后，不出现重复反斜杠、分隔符叠加或公式结构损坏。（F24）
- AC21：遇到缺少闭合分隔符、未知命令或结构不完整的公式时，其余内容仍正常显示，异常片段显示原始内容及“公式需检查”提示，页面可继续切题、保存和确认。（F25）
- AC22：标准答案或解析因关联答案条目而锁定时，其中的公式正常渲染但不能编辑；解除或更换关联后，编辑权限与现有规则一致。（F26）
- AC23：断开外部网络后公式显示、字体和可视化编辑器仍可正常使用；含有 HTML 或脚本片段的识别文本只按文本显示，不执行代码或加载远程资源。（N11-N12）
- AC24：在桌面版 Chrome 和 Edge、1280×720 窗口下，长公式不会撑破中间审核栏，页面可正常滚动，上一题、下一题、保存修改和确认本题按钮始终可达。（N13-N15）
- AC25：只使用键盘能够进入公式编辑、选择常用结构、保存或取消；解析失败状态包含文字提示。新增单元测试和交互测试全部通过，现有前端测试、类型检查和生产构建无回归。（N16-N17）
## 增量需求：作业批改 Agent

### 背景与范围

现有系统已经能够识别原始试卷、识别参考答案并完成题目匹配，也能够保存题目区域与答案区域。下一阶段需要在此基础上增加学生作业批改能力：接收学生在同版原始试卷上的作答，按题定位和识别学生答案，依据题型选择批改工具，执行评分检查与教师复核，并生成带批注的试卷副本和面向学生的错题分析。

首版只支持学生直接在与原始试卷相同版式的试卷上作答，不支持独立答题卡、题序变化或一张图片包含多名学生。首版支持单选题、多选题、填空题和计算题。系统采用“确定性规则与工具优先、大模型处理语义判断、审计规则约束最终结果”的受控工作流；大模型不得绕过题型规则、评分细则、审计或教师复核直接写入最终成绩。

### 目标

- 将学生试卷页面与已确认的原始试卷对齐，并复用现有题目区域提取每题作答。
- 对填空题逐空切分和评分；对计算题保留完整作答区域，并支持一个题目对应多个区域或跨页区域。
- 按已确认题型路由到单选、多选、填空或计算题批改器。
- 单选题按完全匹配评分；多选题实现“少选按正确选项比例得分、错选零分”，每题最终得分保留两位小数。
- 填空题每空独立计分：规范化后完全一致由规则判定；所有非完全一致答案交由大模型判断，数值和公式工具同时提供可验证证据。
- 根据题目、标准答案、答案解析和满分生成计算题评分点草案，由教师编辑、确认并冻结后使用。
- 计算题采用严格依赖评分：某评分点错误时，所有直接或间接依赖它的后续评分点均不得分；独立评分点不受影响。
- 只将低置信度、工具冲突、评分矛盾、证据不足、未覆盖的新解法或模型无法判断的结果送教师复核。
- 保留学生原始试卷不变，另行生成批注副本：满分题绿色勾，零分题红圈错误位置，部分得分题橙色三角和分数并红圈失分位置。
- 生成学生错题分析：按错题说明得分、错误位置、错误原因、相关知识点和简短改进建议，不直接展开完整答案或完整解题过程。
- 教师修改识别结果或评分后，自动使旧报告和旧批注失效并重新生成。
- 保存评分依据、模型与工具结果、审计过程、教师修改和最终结果，保证全过程可追溯。

### 功能需求

#### 学生试卷接收、对齐与作答切分

- **F27**：教师可以在一个已经确认题目、标准答案和题型的作业任务下创建学生提交，填写学生标识并上传学生试卷图片或 PDF。
- **F28**：系统应校验学生试卷与原始试卷的页数、页序、页面完整性和清晰度；发现缺页、重复页、严重模糊或无法对应的页面时，不得静默评分，应记录问题并进入人工处理状态。
- **F29**：系统应将每张学生试卷页面与对应原始试卷页面进行版面对齐，保存坐标变换、对齐置信度和失败原因。
- **F30**：系统应基于已确认的题目区域从对齐后的学生试卷中提取每题作答；计算题存在多个作答区域或跨页区域时，应合并为同一题的有序作答证据。
- **F31**：填空题应按已确认的空位切分为独立字段，并为每个空保存位置、分值、学生答案、识别置信度和异常信息。
- **F32**：系统必须保留原始学生试卷文件不变；所有题目裁剪、错误位置和批注坐标均应能够映射回原始页面与原始坐标。
- **F33**：系统应逐题识别学生作答，并保存识别文本或公式、选项集合、来源区域、空位序号、置信度和识别问题。
- **F34**：首版只允许单选题、多选题、填空题和计算题；批改器必须依据教师已确认的题型路由，大模型不得自行改变题型。
- **F35**：空白、不可辨认、被遮挡或明显未完成的作答应明确记录，系统不得猜测学生答案。
- **F36**：不同题目的处理应相互隔离；单题识别或评分失败不得导致其他已完成题目的结果丢失。

#### 计算题评分细则

- **F37**：系统应根据题目、标准答案、答案解析和题目满分生成计算题评分点草案。
- **F38**：每个评分点必须包含唯一标识、判定标准、分值、显示顺序和所依赖的前置评分点；所有评分点分值之和必须等于题目满分。
- **F39**：系统应校验评分点标识唯一、依赖目标存在、依赖关系无环且分值合法；校验失败的评分细则不得确认或冻结。
- **F40**：教师可以新增、删除、修改和排序评分点，也可以编辑评分点之间的依赖关系。
- **F41**：只有教师确认并冻结的评分细则版本可以用于正式评分；冻结后不得原地修改，后续调整必须创建新版本，并保留旧版本及其历史评分结果。

#### 客观题与填空题评分

- **F42**：单选题仅当学生选项与正确选项完全一致时得满分；选错、多选、空白或无法判定均不得自动得分。
- **F43**：多选题完全匹配正确选项集合时得满分；只要包含任一错误选项即得零分；空白得零分。
- **F44**：多选题选择了非空的正确选项真子集且未选择错误选项时，得分为“题目满分 × 已选正确选项数 ÷ 正确选项总数”。
- **F45**：多选题的比例得分应使用精确十进制计算，并按标准四舍五入保留两位小数；系统同时保存原始比例、未舍入得分和最终得分。
- **F46**：填空题每个空具有独立分值，应逐空识别、逐空判断、逐空给分，题目得分为各空得分之和。
- **F47**：填空答案经过可配置的基础规范化后与教师配置的标准答案或同义答案完全一致时，由确定性规则直接判定该空得满分。
- **F48**：所有规范化后非完全一致的填空答案均应交由大模型判断；模型必须返回“正确、错误或无法判断”、判断依据、简短原因和置信信息，不得只返回一个分数。
- **F49**：对于数值和公式填空，系统还应分别运行支持科学计数法与单位换算的数值验证工具，以及数学等价验证工具，并将结果作为大模型判断的证据；模型与验证工具冲突时必须进入教师复核。
- **F50**：单个填空仅允许满分、零分或待复核，不提供空内部分分。

#### 计算题评分与依赖传播

- **F51**：计算题必须使用本次评分运行绑定的冻结评分细则版本，评分期间不得临时修改评分点或分值。
- **F52**：批改器应对每个评分点分别输出“满足、不满足或无法判断”、所得分值、学生作答中的证据位置、错误或判断原因及置信信息。
- **F53**：每个自动得分的计算题评分点必须有可定位到学生作答区域的证据；不得仅根据最终答案反推学生写出了未呈现的中间步骤。
- **F54**：计算题采用严格依赖规则：任一评分点判定为不满足或因依赖失效而得零分时，所有直接或间接依赖该评分点的后续评分点均得零分，即使后续形式上写对。
- **F55**：与错误评分点不存在直接或间接依赖关系的独立评分点仍应正常判定和计分。
- **F56**：计算题题目得分为有效评分点得分之和，必须大于等于零且不得超过题目满分。

#### 评分审计与教师复核

- **F57**：系统应在生成最终成绩前检查题目得分边界、分项和总分一致性、空白作答、依赖传播、证据完整性、评分细则覆盖情况及模型与工具冲突。
- **F58**：仅在低置信度、模型与确定性工具冲突、依赖或分数矛盾、证据不足、评分细则未覆盖的新解法、作答不可辨认或模型无法判断时进入教师复核；不得默认把所有计算题和非完全一致填空题都交给教师。
- **F59**：教师复核页面应同时展示题目、标准答案、相关评分细则、学生作答原图与识别结果、自动评分、证据位置、触发复核的原因和工具结果。
- **F60**：教师可以修改学生答案识别结果、填空判定、计算题评分点判定、错误位置、错误原因和最终结论；修改后系统必须重新执行分数、依赖和一致性检查。
- **F61**：只有当提交中不存在待复核项目且所有审计检查通过时，系统才可以将本次评分运行标记为已完成。

#### 试卷批注

- **F62**：系统应在不修改原始学生试卷的前提下生成独立的批注试卷副本。
- **F63**：题目获得满分时，在该题作答区域附近绘制绿色勾。
- **F64**：题目得零分时，应使用红色圆圈或椭圆圈出错误答案位置；计算题应优先圈出第一个导致失分的错误步骤。
- **F65**：题目获得部分分时，应绘制橙色三角符号并标注“得分/满分”，同时用红圈标出错误、遗漏或第一个导致后续失分的步骤。
- **F66**：待教师复核的题目不得绘制最终正确或错误标记，只能显示中性的“待复核”提示。
- **F67**：批注不得遮挡学生答案、题干或图形；空间不足时应使用引导线连接批注与证据位置。
- **F68**：教师修改识别结果、评分、错误位置或复核结论后，旧批注副本应标记为过期，并根据最新最终结果重新生成。
- **F69**：教师可以在页面中预览并下载包含全部页面的最终批注试卷。

#### 学生错题分析与反馈

- **F70**：系统应基于最终确认的评分结果生成面向学生的错题分析报告。
- **F71**：每道错题应至少包含题号、题型、得分与满分、错误位置裁剪图、错误原因、相关知识点和简短改进建议。
- **F72**：部分得分题的反馈应区分已掌握部分与首个失分点；计算题因依赖关系被连带扣分时，应明确区分“本步骤自身错误”和“受前置错误影响未得分”。
- **F73**：错题分析不得重新评分，其得分、错误位置和错误原因必须与最终确认结果一致。
- **F74**：反馈不得直接展开完整标准答案或完整解题过程，只提供足以帮助学生定位问题和改进的简短提示。
- **F75**：满分题默认不进入逐题错题列表，但报告可以展示总分、题型得分和正确题数等简要汇总。
- **F76**：教师修改最终评分后，旧错题分析应标记为过期并按最新结果重新生成。
- **F77**：教师可以预览并下载学生错题分析报告。

#### 任务状态、持久化与审计

- **F78**：评分运行应至少展示上传、页面校验与对齐、作答切分、作答识别、自动评分、评分审计、等待复核、生成批注、生成报告和完成等状态。
- **F79**：系统应保存原始文件、题目与空位区域、识别结果、评分细则版本、模型输出、工具结果、审计结果、教师修改、最终分数和生成物之间的关联。
- **F80**：同一学生提交重新评分时，应创建新的评分运行并保留旧运行、旧结果和旧生成物，不得覆盖审计历史。
- **F81**：系统应记录模型提供方、模型名称、提示词版本、结构化输出版本、耗时、重试次数和错误摘要，但不得记录 API 密钥。
- **F82**：评分运行中断后应能够从已保存阶段恢复或安全重试，已经完成且输入未变化的题目不得重复计分或丢失结果。

### 非功能需求

- **N18**：相同页面、原始模板和对齐配置应产生可复现的坐标变换；低质量对齐不得被伪装为成功。
- **N19**：选项集合比较、多选比例、分数求和、上限检查和依赖传播必须由确定性代码实现，不得交给大模型计算。
- **N20**：涉及分数的计算必须使用十进制语义，避免二进制浮点误差；最终每题得分保留两位小数。
- **N21**：所有大模型响应必须使用版本化结构化格式，并在使用前完成字段、枚举、分值和引用完整性校验；无效响应不得进入最终结果。
- **N22**：大模型自报置信度只可作为复核信号之一，不能单独决定自动通过。
- **N23**：自动通过阈值、识别阈值和工具冲突策略应可配置并通过验证集校准；未校准的新模型或新提示词默认采用更保守的复核策略。
- **N24**：原始文件、识别结果、自动评分、教师修改和最终结果应分层保存，任何派生结果都不得覆盖原始证据。
- **N25**：冻结评分细则必须不可变且带版本号；每个计算题结果都必须引用其实际使用的版本。
- **N26**：每个题目的识别、评分、审计、批注和反馈操作应具有明确输入版本并支持幂等重试。
- **N27**：一个学生提交使用一个父级评分运行；逐题任务可在受控并发度下执行，不得无限并发调用模型或图像工具。
- **N28**：外部模型调用必须配置超时、有限重试和失败分类；重试不得覆盖已经完成的有效结果。
- **N29**：学生身份信息和原始试卷应默认保存在本地；调用外部模型时只发送完成当前判断所必需的题目和作答内容。
- **N30**：日志和模型请求记录不得包含 API 密钥、无关学生身份信息或不必要的完整原始文件。
- **N31**：批注副本应保持原试卷页数、页序、清晰度和可读性，网页预览与下载文件中的批注位置应一致。
- **N32**：当错误位置坐标不可靠时，系统不得绘制猜测性红圈，应将题目标记为待复核或只使用题级中性标记。
- **N33**：批注不能只依赖颜色表达结论，还应同时使用勾、圆圈、三角、文字或分数等形状与文本信息。
- **N34**：批注线宽、透明度、字号和引导线应在常见拍照与扫描分辨率下可辨认，并尽量避免遮挡原始内容。
- **N35**：错题报告中的裁剪图必须来自最终评分证据所引用的实际学生作答区域。
- **N36**：学生反馈应使用简洁、具体、适龄且非羞辱性的表达；错误原因与建议必须由最终评分证据支持。
- **N37**：教师修改后，旧批注和旧报告应明确标记为过期，在新文件生成完成前不得作为最新结果下载。
- **N38**：教师工作台应支持最新版 Chrome 和 Edge，并在 1280×720 及以上桌面分辨率下完成主要流程。
- **N39**：系统应按学生、题目和处理阶段记录耗时、模型用量、重试次数、失败类型和复核触发原因。
- **N40**：离线评测应分别统计页面对齐、作答识别、客观题评分、填空题判断、计算题评分点、复核召回率和错误位置定位指标，不得只统计总分误差。
- **N41**：所有确定性评分规则、舍入、依赖传播、状态转换和失效重生逻辑必须有自动化测试。
- **N42**：端到端测试应覆盖多选满分、少选部分分、错选零分、多空填空、非完全一致填空、计算题依赖扣分、低置信度复核及教师修改后重新生成。
- **N43**：现有合成评分基准的标签必须经过教师人工确认后，才能用于正式自动通过阈值和准确率验收。

### 首版明确不做

- 不支持独立答题卡、答题纸、题序变化或学生自定义作答区域。
- 不支持判断题、证明题、作文题、连线题、作图题等其他题型。
- 不支持从一张图片自动拆分多名学生试卷，也不自动识别或推断学生身份。
- 不承诺自动处理严重缺页、乱序、遮挡、模糊、折叠或无法与原卷对齐的试卷；此类情况进入人工处理。
- 不允许在标准答案未确认或计算题评分细则未冻结时执行正式评分。
- 不允许为了某个学生临时修改冻结的评分细则；规则变化必须创建新版本并重新评分。
- 计算题首版不采用“后续方法正确仍给方法分”的跟随错误策略，而是执行严格依赖扣分。
- 单个填空不提供部分分。
- 不允许大模型绕过确定性题型规则、评分细则、审计或复核直接写入最终成绩。
- 不根据不可辨认或未写出的内容猜测答案和步骤。
- 不联网搜索标准答案，也不在标准答案或评分依据缺失时让模型自行解题后作为正式依据。
- 学生反馈不展示完整标准答案或完整解题过程。
- 不修改、覆盖或删除学生原始试卷；所有标注都生成独立副本。
- 待复核题目不绘制最终正确或错误标记。
- 不依赖一次大模型调用或大模型自报置信度自动放行高风险结果。
- 首版不包含班级排名、班级统计、长期学情趋势和知识图谱推荐。
- 首版不新增账号体系、云端多租户或多教师实时协作。
- 首版不自动打印、发送或发布批改结果，只提供本地预览和下载。
- 首版不向用户暴露通用多 Agent 编排器或自由循环；具体编排技术在实现计划中确定。

### 验收标准

- **AC26**：上传与原卷同版的完整学生试卷时可以创建提交；缺页、重复页、严重模糊或无法对应的页面会被明确阻断或转人工处理，不产生静默最终分。
- **AC27**：同一学生页面与同一原卷重复对齐时，题目区域能够稳定映射回原始页面；对齐低于阈值时不会继续绘制猜测性批注。
- **AC28**：系统能为每题生成独立作答证据，填空题能拆为独立空位，具有多个区域或跨页作答的计算题能按正确顺序聚合。
- **AC29**：每题识别结果包含内容、区域、置信信息和异常；故意使一题识别失败后，其他题目的已完成结果仍被保存。
- **AC30**：已确认的四种题型分别进入对应批改器，模型不能改变题型；遇到不支持题型时明确阻断正式评分。
- **AC31**：计算题评分细则草案可由教师编辑；只有评分点唯一、分值和等于满分且依赖无环的版本才能冻结并用于评分。
- **AC32**：冻结评分细则后再修改会创建新版本，旧版本和旧评分结果仍可查看，相关提交被标记为需要重新评分。
- **AC33**：单选题测试覆盖完全匹配得满分、错误选项得零分、多选得零分和空白得零分。
- **AC34**：正确选项为 `ACD`、满分为 6 分时，学生选择 `ACD` 得 6.00 分，选择 `AC` 得 4.00 分，选择 `A` 得 2.00 分，选择 `AB` 得 0.00 分，空白得 0.00 分。
- **AC35**：多选题出现不能整除的比例时使用精确十进制计算并按标准四舍五入保留两位小数，同时可追溯原始比例和未舍入值。
- **AC36**：一个包含多个空且各空分值不同的填空题能够逐空给满分、零分或待复核，题目总分等于各空最终分数之和。
- **AC37**：规范化后完全一致或命中教师配置同义答案的填空由规则直接判定；所有其他答案产生结构化大模型判断、依据和原因。
- **AC38**：科学计数法、可换算单位和数学等价公式分别能够调用对应验证工具；验证工具与模型结论冲突时不能自动完成，必须进入教师复核。
- **AC39**：每道计算题的每个评分点都产生状态、得分、作答证据、原因和置信信息；没有证据的自动得分会被审计拦截。
- **AC40**：构造评分点 `P1 → P2 → P3` 且另有独立 `P4`：当 `P1` 错误时，`P1、P2、P3` 均为零分，`P4` 仍按学生作答正常评分。
- **AC41**：任一题分项之和与题目得分一致，题目得分不小于零且不超过满分，总分与各题最终分之和一致。
- **AC42**：低置信度、工具冲突、依赖或分数矛盾、证据不足、新解法未覆盖、不可辨认和模型无法判断均能触发复核；普通高置信度且无冲突结果不会无条件进入复核。
- **AC43**：复核页能同时查看题目、答案、评分细则、学生原图、识别结果、自动评分、证据和触发原因，并可完成必要修改。
- **AC44**：教师修改识别或评分结果后，系统重新执行依赖和分数审计；仍有矛盾时不能将提交标记为完成。
- **AC45**：满分题生成绿色勾，零分题生成红圈，部分分题生成橙色三角与“得分/满分”并圈出失分位置，待复核题不生成最终正误标记。
- **AC46**：零分或部分分题的红圈能够定位到学生实际错误答案、遗漏位置或计算题第一个错误步骤；坐标不足时转复核而不是绘制虚假红圈。
- **AC47**：批注副本与原试卷页数和页序一致，标记不遮挡主要答案内容，网页预览与下载文件位置一致，并可通过形状或文字在非彩色情况下辨认。
- **AC48**：教师修改评分或错误位置后，旧批注被标记为过期，重新生成的文件反映最新最终结果，原始学生试卷未被修改。
- **AC49**：每道非满分题在错题报告中包含题号、题型、得分、满分、真实错误裁剪图、错误原因、知识点和简短建议，且与最终评分一致。
- **AC50**：部分分计算题报告能区分学生已掌握部分、首个失分点和因依赖关系连带失分的后续点，且不展示完整答案或完整解题过程。
- **AC51**：教师能够预览并下载完整批注试卷和错题分析报告。
- **AC52**：评分运行可展示各处理阶段和当前状态；发生复核时能准确显示待处理题目和原因。
- **AC53**：同一提交重新评分会产生新的运行记录，旧运行的输入版本、评分细则、模型结果、教师修改和生成物仍可追溯。
- **AC54**：模拟模型超时或临时失败时，系统按配置有限重试；重试成功后不会产生重复评分项，重试失败时保留已完成题目并给出可恢复状态。
- **AC55**：在评分中途终止服务并重启后，可以从已持久化阶段恢复或安全重跑，已完成且输入未变化的题目结果不会丢失。
- **AC56**：运行日志、导出结果和模型请求记录中不出现 API 密钥，也不发送与当前题目判断无关的学生身份信息或整份原始文件。
- **AC57**：大模型返回缺字段、非法枚举、越界分数或不存在的评分点引用时，结果会被结构校验拒绝并重试或转复核，不能进入最终成绩。
- **AC58**：相同输入、相同冻结规则和相同工具版本重复执行确定性评分、舍入和依赖传播时产生完全一致的结果。
- **AC59**：最新版 Chrome 和 Edge 在 1280×720 及以上分辨率可完成上传、查看进度、复核、预览和下载；批注不只依赖颜色传达结论。
- **AC60**：可以按学生、题目和阶段查询耗时、模型用量、重试次数、失败类型及复核触发原因，并能定位一次最终评分使用的模型、提示词和评分细则版本。
- **AC61**：自动化测试覆盖全部确定性评分规则、两位小数舍入、严格依赖传播、审计、状态转换和教师修改后的生成物失效逻辑；端到端测试覆盖本增量需求规定的主要流程。
- **AC62**：使用经过教师确认标签的基准集完成页面对齐、识别、四类评分、复核和错误位置定位评测；未经人工确认的现有合成标签不得用于宣称正式准确率或设置自动通过阈值。

## 增量需求：非重叠识别、边界合并与重复题生命周期

### 背景

当前 PDF 识别采用重叠批次，同一边界页会被识别两次，可能形成重复题；对于真正跨页的题，两次输出还可能各自不完整，单纯按相似度去重无法可靠恢复完整题目。

### 目标

- 主识别批次改为互不重叠，使普通题和普通答案条目只识别一次。
- 对相邻批次的边界页进行专门的模型判断，分别合并跨页题目和跨页答案解析。
- 模型只依据原始边界页合并，禁止解题、补造内容或改写原意。
- 合并失败或置信不足时保留待审核结果，不静默丢题或丢答案。
- 审核页允许教师将漏网的重复题标记为重复，使其退出有效题目列表、统计、匹配和完成校验。
- 重复题的原始识别数据和操作审计继续保留，并支持恢复。

### 功能需求

- **F83：非重叠主识别**：试卷与答案 PDF 的主识别批次互不重叠，每一页在主识别阶段只进入一个批次。
- **F84：边界页复核**：每两个相邻批次之间，系统把前一批最后一页、后一批第一页及两侧识别草稿提交给模型，专门判断是否存在跨页题目或跨页答案解析。
- **F85：按文档角色合并**：试卷边界合并题号、题干、选项、题型、分值、来源页和区域；答案边界合并题号提示、答案、解析、题干提示及来源页。合并结果不得解题、改写原意或添加页面中不存在的内容。
- **F86：保留完整证据**：合并后的条目记录所有相关来源页；原始批次响应、边界合并响应、模型版本、用量及异常信息分层保存，不能覆盖原始识别记录。
- **F87：安全降级**：边界模型调用失败、返回非法结构、无法确定连续关系或置信不足时，不静默删除任何草稿；保留原条目并标记“跨页合并待审核”。
- **F88：最终确定性去重**：边界合并后再执行一次确定性去重，消除题号前缀、公式表示和标点差异造成的完全重复项；同号但内容不同的题目或答案不得被自动合并。
- **F89：有效条目参与匹配**：只有边界合并完成、通过结构校验且未被教师标记为重复的题目，以及有效答案条目，才能进入答案匹配、题目统计及任务完成校验。
- **F90：教师标记重复题**：审核页为每道题提供“标记为重复”操作。执行前显示确认提示；成功后该题立即退出正常题目列表、统计、匹配和完成校验，其已占用答案重新变为可用。
- **F91：查看与恢复**：审核页提供“重复题”筛选。教师可查看被标记题目的原始内容并恢复；恢复后的题目进入待确认状态，重新参与答案匹配，不直接恢复可能已经过期的匹配结论。
- **F92：并发与下游保护**：学生答卷正在处理时禁止标记或恢复题目；已有非运行中学生结果时，题目集合变化应将相关结果标记为需要重新处理，旧批注和报告不得继续作为当前结果。
- **F93：审计**：标记重复、恢复、自动边界合并和合并降级均记录可追踪的审计事件。

### 非功能需求

- **N44：数据完整性**：原始 PDF、页面图像、原始模型响应和被标记为重复的记录不得被物理删除或覆盖。
- **N45：确定性与幂等性**：使用相同模型响应重复执行合并、去重或恢复操作，不得产生额外副本；同一题或答案最多形成一个有效条目。
- **N46：模型调用约束**：每个相邻批次边界、每种文档角色最多发起一次边界合并流程；重试遵循现有超时、重试次数和并发限制。
- **N47：输出安全**：边界模型输出必须经过严格结构、题号、来源页和字段类型校验；异常输出不能进入答案匹配。
- **N48：可观测性**：运行记录能够区分主识别与边界合并，并显示调用次数、Token 用量、失败边界及降级原因。
- **N49：兼容现有任务**：数据升级后，历史题目默认保持有效；历史任务仍可打开、审核、标记重复和恢复，不要求重新上传文件。
- **N50：操作一致性**：标记或恢复成功后，题目导航、统计、未使用答案数量和完成按钮状态必须在一次刷新内保持一致。
- **N51：可访问性**：标记、确认和恢复操作具有明确文本、键盘焦点和处理状态；危险操作不能只依靠颜色表达。
- **N52：失败可恢复**：边界调用中断不得破坏已经保存的主识别结果；任务重试后可以重新完成边界合并。

### 本轮不做的事

- 不让模型跨越非相邻批次任意搜索和合并题目。
- 不把“同题号”直接视为同一道题；内容冲突时必须保留并进入审核。
- 不允许边界合并模型解题、生成标准答案或补造页面不可见内容。
- 不物理删除重复题、原始答案、模型响应或审计记录。
- 不自动重新运行全部历史任务；历史任务可人工标记重复，重新识别需通过现有重试流程触发。
- 不在本轮提供模型、提示词、合并阈值的前端配置界面。
- 不把这套边界合并用于学生作答识别、评分或批注合并。
- 不在模型结论不确定时自动选择并隐藏其中一道题。
- 不支持跨任务合并题目或答案。

### 验收标准

- **AC63（F83）**：对多批次试卷和答案运行识别后，主识别记录中每个页码恰好出现一次，不存在主批次重叠页。
- **AC64（F84）**：存在三个主批次时，试卷和答案分别产生两个边界合并记录，每条记录只包含对应边界的相邻两页及两侧草稿。
- **AC65（F85）**：一道题从前页开始、后页结束时，最终只生成一道有效题目，题干、选项、分值、题型、来源页和区域包含两页可见信息。
- **AC66（F85）**：一条答案解析跨页时，最终只生成一个有效答案条目，答案和解析按页面内容正确衔接，不重复拼接相同文本。
- **AC67（F86）**：查询运行数据时，可以同时取得主识别原始响应、边界合并原始响应、最终条目、模型信息、Token 用量和来源页。
- **AC68（F87、N47、N52）**：模拟边界模型超时、非法 JSON、错误页码及“不确定”结论时，主识别草稿全部保留，条目带有“跨页合并待审核”异常，匹配流程不会使用非法合并结果。
- **AC69（F88）**：同一道题分别带有“10.”前缀、LaTeX 公式和 Unicode 公式时，最终只保留一道有效题。
- **AC70（F88）**：两个题号相同但题干含义不同的条目仍分别保留，并显示题号冲突，不发生自动合并。
- **AC71（F89）**：答案匹配输入中不包含已标记为重复的题目；真实复现场景中原来的 17 道题收敛为 15 道有效题，匹配不再因第 10、12 题重复而冲突。
- **AC72（F90、N50、N51）**：教师在审核页点击“标记为重复”并确认后，该题退出普通导航和题目总数，其匹配答案立即变为未使用；操作具有忙碌、成功和失败反馈。
- **AC73（F91）**：切换到“重复题”筛选能够看到该题及原始内容；点击恢复后，它回到普通导航并处于待确认状态，旧匹配不会直接恢复。
- **AC74（F92）**：学生答卷处于对齐或识别状态时，标记和恢复请求返回明确冲突；已有非运行中结果时，操作成功但相关学生结果、批注和报告被标记为需要重新处理或已过期。
- **AC75（F93、N44）**：自动合并、合并降级、标记重复和恢复均生成包含任务、题目或答案标识及操作者的审计事件；被标记题目的数据库记录和原始识别数据仍存在。
- **AC76（N45、N46）**：对同一批模型响应重复执行整理不会增加有效题目、答案或边界记录数量；模型调用次数不超过相邻批次边界数，并遵循配置的重试和并发限制。
- **AC77（N49）**：升级前创建的历史任务可以正常打开，所有历史题目初始均有效，并可完成标记重复与恢复操作。
- **AC78（端到端）**：上传包含跨页题目和跨页解析的试卷—答案组合，依次完成非重叠主识别、边界合并、确定性去重、答案匹配、人工标记漏网重复题、恢复及完成审核；全过程无原始数据丢失，页面统计与后端状态一致。

## 增量需求：多空填空题评分配置初始化

### 背景

当前评分设置在填空题尚未保存配置时固定只创建一个空位 `B1`，并把整条参考答案作为 `B1` 的一个标准答案。因此，题干中有多个空的题目仍只显示一个评分空位，各空答案无法独立设置分值、答案类型、同义答案和评分结果。

现有真实数据还表明，答题区域识别可能把同一道多空填空题返回为一个覆盖整题的复合区域。例如一道题题干含三个明确下划线空位、答案为“失去 异种 吸引”，但只保存了一个答题区域。因此不能只依赖区域数量，还需要把题干空位标记和参考答案结构作为确定空位数量的补充证据。

### 目标

- 首次打开多空填空题的评分设置时，自动创建与可靠空位数量一致的 `B1`、`B2`……，不再固定只创建一个空。
- 综合独立答题区域、题干中的明确空位标记以及结构化参考答案确定空位数量；单个复合答题区域不能把明确的多个空压缩成一个。
- 按原顺序把参考答案拆分并一一分配给对应空位，避免把所有答案放进第一空。
- 当证据无法可靠对齐时保留教师可编辑的空位，并明确提示检查，不通过猜测、解题或静默合并掩盖歧义。
- 已经由教师保存过的评分配置保持不变，自动初始化只作用于尚无空位配置的填空题。

### 功能需求

- **F94：自动确定空位数量**：填空题尚无已保存空位配置时，系统应综合独立答题区域数量、题干中的连续下划线或等价明确空位标记数量，以及可明确分组的参考答案数量，生成连续编号的 `B1`、`B2`……。题干中存在多个明确空位时，不得因答题区域只有一个而降为单空。
- **F95：复合区域共享**：当一个答题区域覆盖多个明确空位时，系统应保留多个空位，并允许这些空位共享该复合区域作为初始证据；当独立区域数量与空位数量一致时，按页面和阅读顺序一一绑定。
- **F96：按预期数量拆分答案**：系统应先确定空位数量，再按参考答案的编号、换行、分号及其他明确结构从左到右拆分。只有拆分结果数量与预期空位数一致且不会破坏公式、数值与单位时，才自动分配到各空位。
- **F97：受控空白分隔**：当参考答案使用空格分隔且没有更强结构时，只有在可得到与预期空位数完全一致、每段均非空且不会明显拆开数值与单位或公式的情况下才自动拆分；否则不得把所有内容继续塞入 `B1`，而应创建正确数量的空位并提示教师检查答案分配。
- **F98：逐空标准答案**：可靠拆分后，每个空位的初始标准答案只包含属于该空的答案。例如三个空的参考答案“失去 异种 吸引”应初始化为 `B1=失去`、`B2=异种`、`B3=吸引`。
- **F99：默认分值守恒**：自动创建多个空位时，默认分值应尽量均分并采用十进制精确计算；因两位小数产生的余数分配给最后一个空位，确保所有空位分值之和严格等于本题满分。
- **F100：歧义提示与人工修正**：空位数量或答案分配存在歧义时，评分设置应显示明确提示，指出预期空位数及需要检查的空位；教师仍可增加、删除空位并修改每空分值、类型、标准答案和同义答案。
- **F101：已保存配置优先**：只要题目已有一个或多个已保存空位定义，后续打开评分设置必须原样读取该配置，不得因题干、区域或参考答案变化而自动重建或覆盖。
- **F102：单空兼容**：只有一个可靠空位且参考答案不需要跨空拆分的题目继续初始化为一个空；非填空题不生成空位定义。

### 非功能需求

- **N53：确定性与幂等性**：相同题干、答题区域、参考答案和满分应始终生成完全相同的空位数量、顺序、答案分配、区域关联、分值和提示；重复打开页面不得不断新增空位。
- **N54：无新增模型成本**：多空初始化使用已有识别数据和确定性规则完成，不为拆分空位或参考答案新增大模型调用，也不联网搜索答案。
- **N55：数据兼容性**：现有已保存的单空和多空配置、历史评分运行、逐空评分结果及审计记录均保持可读取，不要求数据库中的历史配置自动迁移或重写。
- **N56：保守降级**：任何可能破坏公式、数值、单位或答案原意的拆分都必须降级为人工检查，不得以提高自动拆分率为由猜测答案边界。
- **N57：界面一致性与可访问性**：初始化提示、歧义警告和空位编号使用明确文字表达，不能只依赖颜色；键盘用户可以访问每个空位的分值、类型、答案、同义答案和删除操作。
- **N58：性能约束**：初始化只处理当前题目的短文本和少量区域，在普通审核页加载中不产生可感知等待，不阻塞其他题目或评分配置读取。

### 本轮不做的事情

- 不重新调用试卷识别、区域识别或答案识别模型来确定空位，也不新增专门的答案拆分模型。
- 不自动保存刚生成的空位配置；教师检查并点击保存后，配置才成为正式评分输入。
- 不自动覆盖或重建已经保存的空位配置；历史上已保存为单空的题目仍可由教师手动增加空位和重新分配答案。
- 不把逗号、顿号、斜杠或普通空格无条件视为跨空分隔符，避免拆坏公式、单位、向量、选项组合或同一空的多个可接受答案。
- 不根据题目内容自行解题、补造缺失答案、生成同义答案或推断教师未提供的评分规则。
- 不改变现有逐空评分算法、答案类型判定规则、教师复核流程、历史评分结果或生成物版本机制。
- 不在本轮增加批量重建全部历史填空题配置的操作。

### 验收标准

- **AC79（F94、F95、F98）**：题干含三个明确下划线空位、只有一个复合答题区域、参考答案为“失去 异种 吸引”时，首次打开评分设置生成三个空位，依次为 `B1=失去`、`B2=异种`、`B3=吸引`，三个空位均可引用该复合区域，不再把整条答案放入 `B1`。
- **AC80（F96、F97、N56）**：题干含两个空、参考答案为“1×10⁻⁶ 负”时，生成 `B1=1×10⁻⁶`、`B2=负`；科学计数法的指数、乘号及数值本身保持完整。包含“2 m/s”或复杂公式的歧义文本不会被普通空格盲目拆坏。
- **AC81（F94、F95）**：存在三个独立答题区域且题干也有三个空时，生成三个空位，区域按页码、纵向位置和横向位置的阅读顺序分别关联到 `B1`、`B2`、`B3`。
- **AC82（F97、F100）**：可靠空位数为三个但参考答案无法安全拆成三段时，页面仍生成三个可编辑空位，显示“答案分配需要检查”及预期数量；原参考答案继续在题目答案区域可见，任何单个空位都不会默认收到整条未拆分答案。
- **AC83（F99）**：满分为 4 分且有三个空时，默认分值为 `1.33`、`1.33`、`1.34`；满分为 5 分且有两个空时为 `2.50`、`2.50`，所有空位分值始终精确等于本题满分。
- **AC84（F101、N55）**：题目已有教师保存的 `B1`、`B2` 配置后，即使题干标记、区域或匹配答案发生变化，再次打开仍原样返回已保存的空位、分值、答案类型、标准答案和同义答案，不执行自动初始化覆盖。
- **AC85（F102）**：题干只有一个空、一个答题区域和一个参考答案时仍只生成 `B1`；选择题和计算题的评分设置不生成填空空位。
- **AC86（F100、N57）**：教师能够通过键盘依次访问并编辑所有自动生成空位，增删空位、调整答案及分值后可以保存；存在歧义警告时保存仍需满足每空有标准答案且分值之和等于满分的现有校验。
- **AC87（N53、N54、N58）**：相同题目连续打开评分设置两次，初始化结果完全一致且空位数量不增长；请求期间没有新增模型调用，页面加载时间与现有单空初始化处于同一量级。
- **AC88（F94-F102）**：初始化规则具有自动化单元测试，评分配置接口及前端组件具有集成测试，完整后端与前端测试、类型检查、静态检查和生产构建无回归。
- **AC89（端到端）**：从审核页打开一道人类可确认的多空填空题，看到正确数量、答案顺序和总分守恒的初始配置；教师保存后重新打开保持不变，并能够用于一次逐空评分运行。

## 缺陷修复：派生填空配置与正式批改状态一致性

### 背景

评分设置接口会为没有保存空位的填空题返回 `derived` 预览，教师因此能看到完整的 B1、B2……答案和分值；但该预览不写数据库。题目确认和任务完成目前也不校验或保存这些空位，直到创建评分运行时才因数据库没有空位记录而统一报“填空题必须配置每个空”。这使界面显示、任务完成状态和正式批改门禁相互矛盾。

当前运行库已复现：第 9、10、11、12 题均已确认且任务已完成，界面可派生多空预览，但四题 `config_version` 均为空、保存空位数均为 0；审计记录没有对应的填空配置保存事件。

### 目标

- 让安全、完整的派生填空配置在教师执行明确确认动作时成为正式保存配置。
- 让题目确认、任务完成和评分启动使用同一套配置就绪判定，避免前一步成功、后一步才出现笼统错误。
- 兼容已经完成但没有保存填空配置的历史任务，使其在重试评分时可安全补齐，不要求逐题重新打开。
- 对无法安全拆分或缺少逐空答案的题目继续阻断，并准确指出题号和原因。

### 功能需求

- **F103：统一配置就绪判定**：系统应以同一规则判断填空配置是否可正式使用：已有保存空位直接通过；否则派生结果必须空位数大于零、每空均有非空标准答案、每空分值有效且总和等于题目满分。
- **F104：明确动作自动确认安全配置**：教师确认题目、完成任务或启动评分时，如果填空题没有保存配置但派生结果满足 F103，系统应保存该配置并记录审计事件。普通 GET 仍保持只读。
- **F105：历史任务评分前补齐**：对于此前已经确认或完成的任务，评分预检应使用 F103 检查缺失配置；全部安全时先幂等保存，再构建本次评分输入，使当前历史任务重试即可继续。
- **F106：歧义配置精确阻断**：派生结果存在空答案、无效分值或其他需要人工检查的警告时，不得自动保存或进入评分；错误应包含题目 ID、题号、预期空位数和稳定原因代码，不再只返回无法定位题目的笼统提示。
- **F107：任务完成门禁一致**：任务完成前应检查所有有效填空题。所有缺失配置均可安全派生时在同一事务中保存并完成任务；任何一题不安全时任务保持未完成，且不会对本次批量候选产生部分保存。
- **F108：界面状态反馈**：评分设置面板应说明安全派生配置会在确认题目、完成任务或启动评分时自动保存；自动保存后重新读取显示 `source=saved`。阻断错误应引导教师回到对应题目的逐空设置。

### 非功能需求

- **N59：幂等与并发安全**：同一题被重复确认、重复完成任务或重复启动评分时最多保留一套当前空位配置，不重复增加版本或审计事件；并发写入不得形成部分空位。
- **N60：数据保护**：已有保存配置始终优先，自动确认不得覆盖教师保存的空位、分值、答案类型、标准答案、同义答案或区域。
- **N61：事务一致性**：自动确认配置与触发它的题目确认、任务完成或评分预检必须具有明确事务边界；失败时不得留下只有配置头或部分空位的状态。
- **N62：无新增模型调用**：自动确认只复用现有确定性初始化规则，不调用模型、不联网搜索答案。
- **N63：可追踪性**：自动确认审计应记录题目、触发动作、空位数量及初始化信号，并能与教师手动保存区分。

### 本轮不做的事情

- 不自动保存包含空标准答案、分值不守恒或需要人工确定答案边界的配置。
- 不让普通评分配置 GET 产生数据库写入。
- 不覆盖任何已有教师保存配置，也不重写历史评分运行快照。
- 不通过模型补答案、拆答案或判断有歧义的空位。
- 不直接修改当前运行库来绕过应用逻辑；历史数据通过修复后的正常确认或评分预检流程补齐。

### 验收标准

- **AC90（F103、F104）**：未保存但可安全派生的三空题在确认后生成一套保存配置和三个空位，再次 GET 返回 `source=saved`；确认前连续 GET 仍不写数据库。
- **AC91（F105）**：构造一个已完成、填空配置为空但三空答案可安全派生的历史任务，创建评分运行时自动保存配置并成功进入评分，不再返回 `FILL_BLANK_CONFIG_REQUIRED`。
- **AC92（F106）**：三空题只能安全确定数量、无法安全分配答案时，确认、完成任务和评分启动均不自动保存，错误详情包含题号、预期三空及 `answer_split_ambiguous`。
- **AC93（F107、N61）**：任务包含两道安全填空题和一道歧义填空题时完成失败，三道本次候选均不产生部分自动保存；修正歧义题后再次完成，三题配置与任务状态在同一事务中提交。
- **AC94（N59、N60、N63）**：重复执行确认、完成或评分预检不会新增重复空位、无意义版本或重复自动确认审计；已有教师配置在所有触发动作后保持逐字段不变。
- **AC95（F108）**：前端显示自动确认说明；后端返回阻断详情时，页面显示具体题号和“逐空检查并保存”，不再只显示无法定位的通用提示。
- **AC96（N62）**：上述确认、完成和评分预检流程的模型客户端调用次数均为 0，完整后端与前端测试、类型检查、静态检查和生产构建无回归。

## 增量需求：学生答卷自动批改、证据可见性与整页工作台

### 背景

当前学生答卷上传后会完成页面配准、题框映射和作答识别，但教师仍需进入批改工作台手动点击“开始批改”；低置信度识别还会在批改运行创建前形成前置阻断，导致教师需要先处理学生作答审核，不能形成上传后自动批改的连续流程。

现有批改运行已经保存题级证据。对当前运行库的最新完成批改进行只读核查，15 道题均保存了 1–4 个证据区域，但前端只把证据作为默认关闭的蓝色覆盖框，没有可读的识别文本、来源页、判分结论和证据预览，也不会在切换题目后自动跳到证据所在页。因此用户会看到判分结果，却看不到能够解释该结果的“批改证据”。

当前批改工作台按固定三栏布局展示，试卷画布按栏宽缩放并保留内部纵向滚动。长页面不能在可用视口中完整出现，页面滚动、画布滚动和右侧详情滚动同时存在，容易发生误滑。现有进度条仅在运行中的少数状态短暂显示，运行结束或进入待复核后消失，也没有清晰呈现上传处理、逐题批改和生成结果文件之间的阶段关系。

后端目前没有统一的应用日志初始化、请求关联标识和流水线结构化事件；未处理异常也没有写入堆栈。出现自动处理失败、证据缺失或生成物失败时，只能依靠数据库状态猜测原因。

### 目标

- 学生答案卷上传后，在页面配准和题框映射可靠完成、正式评分配置满足现有安全门禁时，自动继续识别、批改、审计和结果文件生成，不要求教师先审核学生作答或手动点击“开始批改”。
- 低置信度、模型无法判断或证据不完整的题目先形成可查看的批改结果，再进入批改后的教师复核队列；教师复核只处理风险题，不成为整份答卷开始批改的前置步骤。
- 让每道题的证据在工作台中默认可见、可读、可定位，并明确关联学生作答、标准答案、判分规则、判分结论和原图位置。
- 让批改进度在处理中、待复核、完成和失败状态均持续可见，能够区分当前阶段、逐题进度和最终状态。
- 默认在无需上下滚动的情况下看到完整单页试卷，同时提供明确的整页适配、宽度适配、缩放、翻页和聚焦查看操作。
- 建立安全、可关联的后端日志系统，并为本轮新增或修改的复杂业务边界补充解释“为什么”的注释和文档字符串。

### 功能需求

- **F109：上传后自动串联批改**：学生答卷上传触发的后台流程在页面准备、配准、题框映射和作答识别完成后，应自动创建并执行绑定当前学生处理版本的批改运行，继续完成逐题评分、整卷审计和结果文件生成。教师不需要进入学生答卷页审核题框，也不需要在批改工作台点击“开始批改”。
- **F110：安全门禁与批改后复核**：只有页面配准和题框映射可靠、每个有效题目都有映射区域、标准答案和评分配置满足现有门禁时才自动批改。作答识别的低置信度、不可辨认、模型无法判断和证据不完整不得阻止其他题目进入批改，而应让对应题目以明确原因进入批改后的待复核队列。配准失败、题框缺失、处理版本过期、标准答案缺失或评分配置不安全仍必须阻断，系统不得猜测坐标、答案或分数。
- **F111：自动运行幂等与版本绑定**：同一个学生处理版本最多创建一个有效自动批改运行；上传请求重试、后台任务恢复、人工配准后重新处理和应用重启不得产生并发或重复运行。重新处理得到新版本时，新运行只读取新版本快照，旧运行和旧生成物保留历史但标记为过期。
- **F112：自动批改失败反馈与恢复**：自动批改未启动或中途失败时，学生列表和批改工作台应显示失败阶段、稳定错误代码、可理解说明和可用的下一步操作；可重试错误允许从安全检查点继续，不可重试错误引导教师修正对应配置或配准问题。
- **F113：持久显示批改进度**：批改工作台应始终显示一条可访问的进度条，覆盖排队、预检查、逐题批改、审计、生成批注、生成报告、待复核、完成和失败状态。逐题批改阶段显示已完成题数、总题数和百分比；非逐题阶段显示阶段名称及确定的阶段进度；完成状态保留 100%，待复核状态显示自动批改已完成和待复核数量，失败状态保留最后成功阶段和已完成题数。
- **F114：证据完整性约束**：每个题级判分结果必须关联至少一个有效证据，证据至少包含学生原图页、原图坐标、识别文本和区域标识；判分详情还必须展示标准答案、评分规则或工具、判分原因、得分以及该结论引用的证据。结果缺少有效证据时不得伪装为无风险最终判定，而应标记 `MISSING_EVIDENCE` 并进入批改后复核。
- **F115：证据卡片与原图定位**：批改详情应新增明确命名的“批改证据”区域，默认展示证据缩略图或可验证的原图裁剪、识别文本、页码、判分依据和结论。点击证据应切换到对应页、突出显示准确区域并保持该证据与当前题的关联；当证据为空、坐标无效或预览失败时显示具体原因和恢复建议，不能静默显示空白。
- **F116：整页适配与显式缩放**：试卷查看区默认使用“整页适配”，完整单页应在中央可用视口内显示且不产生纵向画布滚动；同时提供整页适配、宽度适配、放大、缩小、恢复 100% 和聚焦模式。只有用户明确选择宽度适配或放大后才允许画布内部平移或滚动，缩放比例和当前模式应有文字反馈。
- **F117：稳定翻页与防误滑**：切换题目时应自动定位到该题首个证据或题框所在页；普通鼠标滚轮和触控板滚动不得意外切换试卷页或改变缩放。翻页仅通过明确按钮或键盘快捷键触发，绘制错误位置时继续禁用浏览手势，页面和右侧详情的滚动区域必须清晰分离。
- **F118：工作台布局聚焦**：题目列表、试卷查看区和判分详情保持逻辑分区，但应允许折叠两侧栏或一键进入聚焦模式，使试卷获得主要空间。窄视口下不得把三栏强行压缩到不可读宽度，应改为可切换面板，同时保留当前题、当前页、缩放和图层状态。
- **F119：统一应用日志**：后端应在启动时初始化统一日志配置，至少记录时间、级别、事件名、请求关联标识以及可用的任务、学生答卷、学生处理版本、批改运行和题目标识。HTTP 请求、后台任务、学生处理各阶段、自动批改创建与阶段变化、模型调用摘要、重试、复核原因、证据缺失、生成物结果和未处理异常必须产生可检索日志；异常日志应包含堆栈。
- **F120：日志配置与轮转**：日志级别、是否写控制台、日志文件位置和轮转上限应可通过配置控制并有安全默认值。文件日志目录应自动创建；达到上限后进行有界轮转，应用重启不得无界增长或覆盖当前诊断所需的最近日志。
- **F121：必要代码注释**：本轮新增或修改的自动串联门禁、幂等版本绑定、进度换算、证据坐标校验和整页缩放状态逻辑必须包含简洁的文档字符串或行内注释，解释不明显的约束和设计原因；不对显而易见的赋值、样式或简单分支逐行复述代码。

### 非功能需求

- **N64：自动化安全性**：自动串联不得绕过原始试卷题目与标准答案已经确认、题框版本已经确认、评分配置完整、分值守恒及当前处理版本一致等既有门禁；只取消学生作答批改前的人工审核和手动启动动作。
- **N65：可观测性与关联性**：一次上传产生的学生处理和自动批改日志应能通过答卷标识、处理版本和批改运行标识串联；关键状态必须在数据库状态与日志事件中一致，不依赖解析自由文本判断流程结果。
- **N66：隐私与日志安全**：日志不得记录 API Key、Authorization、Cookie、完整提示词、完整模型原始响应、整份学生原图、无关学生身份信息或任意文件内容。学生姓名、学号、识别全文和标准答案默认不进入日志；只记录必要标识、计数、耗时、模型名、用量和稳定错误摘要。
- **N67：证据可信性**：证据预览必须来自评分运行绑定的学生页和保存坐标，不能使用当前新版题框替代历史运行证据，也不能由前端根据题号猜测区域。裁剪坐标必须进行有限数、正尺寸和页面边界校验。
- **N68：进度单调与真实性**：同一次运行的百分比在正常前进时不得倒退，完成前不得显示 100%，失败不得伪装成完成；恢复运行时可以从已持久化检查点继续计算。无可靠细粒度进度的阶段使用明确阶段进度，不能用循环动画冒充精确百分比。
- **N69：界面可访问性**：进度条提供 `progressbar` 语义、当前值和文字状态；证据、缩放、折叠、翻页和图层操作均可由键盘完成并具有可读名称，状态不能只依赖颜色、虚线或图标表达。
- **N70：响应式与滚动稳定性**：在常见 1366×768、1440×900 和 1920×1080 桌面视口中，整页适配模式不产生中央画布纵向滚动；页面主体不出现与画布竞争的第二条纵向滚动条。触控板连续滚动不会误翻页或意外改变缩放。
- **N71：兼容性**：历史学生答卷和历史批改运行仍可读取；没有自动运行关联或证据裁剪地址的旧记录应以明确的兼容提示展示，不得导致页面崩溃。手动重试和教师复核接口继续可用。
- **N72：测试与可维护性**：自动串联、低置信度后置复核、幂等运行、日志脱敏、证据接口、进度换算、整页布局、证据跳转和防误滑行为应具有自动化测试；注释应随约束代码放置并避免重复文档中的实现清单。

### 本轮不做的事情

- 不取消原始试卷题目、标准答案、题框版本和不安全评分配置的确认门禁；本轮取消的是学生答案卷处理完成后的前置人工审核与手动启动。
- 不在配准失败、题框缺失、学生页不完整或版本不一致时强行批改，也不自动确认低质量配准。
- 不取消批改后的风险题教师复核，不把低置信度或无证据结果自动当作最终可信结论。
- 不把整份学生原图、完整模型请求响应或学生身份信息写入日志，也不建立远程日志平台、指标平台或链路追踪服务。
- 不实现自由拖拽的无限画布、任意旋转、浏览器手势翻页或移动端手写批注；本轮聚焦桌面端稳定查看与明确控制。
- 不要求为历史批改重新生成不存在的证据；历史数据只做安全兼容提示，可由教师选择重新处理产生新运行。
- 不对全仓库显而易见代码进行逐行注释，只注释本轮触及且存在业务约束、坐标约定、并发或状态机风险的逻辑。

### 验收标准

- **AC97（F109、F110、N64）**：上传一份页面配准可靠、题框完整且评分配置就绪的学生答案卷后，不进行教师审核、不进入批改页点击按钮，后台自动依次完成处理和批改并产生题级结果；原始题目与答案未确认或题框映射失败时不会自动评分，并显示准确阻断原因。
- **AC98（F110、F114）**：构造一题识别置信度低但页面与证据区域有效的答卷，其他题继续完成自动批改，该题生成带 `LOW_RECOGNITION_CONFIDENCE` 的待复核结果；构造证据为空的题目时生成 `MISSING_EVIDENCE`，不返回无风险最终判定。
- **AC99（F111、N68）**：对同一上传请求重试、重复触发处理并模拟应用重启恢复，当前处理版本最多有一个有效自动批改运行；新处理版本创建新运行并使旧运行过期，两个运行各自读取自己的版本快照。
- **AC100（F112、F119）**：模拟评分配置阻断和结果文件生成失败，学生列表或工作台显示失败阶段、错误代码和下一步操作；可重试失败从最后安全阶段继续，日志中可用答卷和运行标识找到完整失败堆栈。
- **AC101（F113、N68、N69）**：从排队到完成观察同一运行，进度条持续存在、数值单调，逐题阶段显示 `已完成/总题数`；待复核显示“自动批改完成，N 项待复核”，完成显示 100%，失败保留最后进度且具有可访问名称和值。
- **AC102（F114、F115、N67）**：打开当前最新完成运行的 15 道题，每题均在“批改证据”区域看到至少一个证据卡片，包含学生识别文本、页码、判分原因、标准答案或规则、得分和原图裁剪；点击卡片后切换到对应页并高亮数据库中保存的区域。
- **AC103（F115、N67、N71）**：证据坐标越界、页不存在、裁剪失败和历史运行没有证据地址时，接口不读取数据目录外文件，页面显示具体兼容或错误提示且不崩溃；正常证据预览与评分运行绑定的页和区域一致。
- **AC104（F116、F117、F118、N70）**：在 1366×768、1440×900 和 1920×1080 视口打开单页答卷，默认无需上下滑动画布即可看到整页；切换整页、宽度、100%、放大和聚焦模式行为明确，普通滚轮不误翻页或缩放，折叠侧栏后当前题、页码和图层保持不变。
- **AC105（F117）**：选择一题其首个证据位于非当前页时，页面自动切换到对应页并高亮；使用明确按钮和键盘可以翻页，连续触控板滚动不会更换页码；进入错误位置绘制时不会触发浏览平移或页面滚动。
- **AC106（F119、F120、N65、N66）**：启动应用并完成一次上传自动批改后，控制台和轮转文件中存在请求、学生处理、自动运行、逐题汇总、审计和生成物事件，可按标识串联；捕获日志和轮转文件中搜索不到测试 API Key、Authorization、Cookie、学生姓名、学号、完整识别文本或完整原图内容。
- **AC107（F121、N72）**：代码审查能够在自动门禁、幂等绑定、进度换算、证据坐标与整页适配的非直观逻辑旁找到解释约束原因的注释或文档字符串；没有大段逐行复述实现的噪声注释。
- **AC108（F109-F121）**：新增后端单元与集成测试、前端组件测试、类型检查、静态检查、完整测试套件和生产构建均通过，现有手动复核、人工配准修正、生成物预览下载及历史运行查看无回归。
- **AC109（端到端）**：教师上传学生答案卷后留在学生列表观察处理和批改进度，系统自动完成配准、题框映射、识别、逐题评分、审计和结果生成；进入工作台立即看到整页、持久进度状态和首题证据卡片，点击错题证据定位原图，必要时只复核风险题并下载更新后的批注试卷和错题报告。
