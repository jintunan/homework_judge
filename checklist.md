# Python 后端迁移与试卷结构容错解析 Checklist

> 每项必须通过运行代码、比较数据或观察用户可见行为验证。真实百炼场景需要教师显式确认费用；其他项目不得仅凭代码审阅勾选。
>
> 2026-07-26 已完成本机自动化与无计费验收；`[x]` 表示已有执行证据，未勾选项继续保留给教师验收或后续强化测试。

## Python 服务与依赖

- [ ] Python 3.12 隔离环境可以从 `requirements.lock` 完整安装。（验证：在新虚拟环境执行安装并导入全部运行依赖）
- [x] FastAPI 生产进程可以在 Windows PowerShell 启动和优雅停止。（验证：启动后访问健康接口，再发送停止信号并检查退出码）
- [ ] 配置能加载 `.env` 中现有变量，非法端口、阈值、并发和 URL 会阻止启动。（验证：分别使用合法和非法配置启动）
- [x] 模型状态只返回是否配置、模型、区域和 Base URL 状态，不返回 Key。（验证：使用唯一测试 Key 查询并搜索响应）
- [ ] `npm run dev` 同时启动 Python API 和 Vite，任一进程退出时另一个被停止。（验证：启动后终止 API 并检查命令退出）
- [x] `npm start` 只启动 Python 生产后端，不再启动 Express。（验证：检查进程命令行和监听端口）
- [x] 生产服务同时提供 `/api`、React 根页面和前端子路由 fallback。（验证：请求三个地址均返回正确内容）

## SQLite v3 迁移与数据兼容

- [x] 空数据库可以直接初始化最新 v3 schema。（验证：检查 user_version、表、列、索引和外键）
- [x] v2 数据库自动迁移到 v3，并新增版本级解析问题、草稿归一化和结构修复运行支持。（验证：检查迁移后列与 CHECK 约束）
- [x] v3 迁移连续运行两次不改变数据或新增重复对象。（验证：两次运行前后比较 schema 和表计数）
- [ ] 迁移中注入 answer run 复制失败时完整回滚。（验证：user_version、旧表、旧行和列保持迁移前状态）
- [x] 迁移后 `PRAGMA foreign_key_check` 为空。（验证：直接查询）
- [x] 迁移前后任务、文件、答案版本、正式题目、草稿、运行、来源、提交、复核和审计行数一致。（验证：迁移工具输出对比）
- [x] 迁移前后的所有主键 ID 和提交绑定答案版本一致。（验证：按表导出 ID 与关联字段进行集合比较）
- [ ] 迁移前已确认学生报告的逐题分、总分、批注、确认人和答案版本保持不变。（验证：迁移前后 JSON 对比）
- [ ] 迁移前班级统计的确认数、平均分、最高最低分、成绩段和逐题得分率保持不变。（验证：迁移前后 JSON 对比）
- [ ] 发布、修订、教师终评和确认继续使用原子事务。（验证：分别注入中途失败并确认全部写入回滚）
- [ ] Python 与旧数据的时间和 JSON 字段可以无损读取。（验证：查询包含中文、空值、Token 用量和历史响应的记录）
- [x] 正式切换说明明确禁止 Node 与 Python 同时写同一 SQLite。（验证：README 操作步骤审阅并执行切换演练）

## PDF、图片与文件安全

- [ ] JPG、JPEG 和 PNG 可以纠正方向、转为 RGB、缩放并编码为 JPEG data URL。（验证：使用带 EXIF 方向和透明通道的图片）
- [ ] 最小单页 PDF 可以渲染为一页 JPEG。（验证：检查页码、MIME 和 byte length）
- [x] 本次 7 页高中物理 PDF 输出连续第 1–7 页。（验证：断言页码数组并保存首尾页）
- [x] 本次 PDF 首尾页的中文、公式和示意图清晰可读。（验证：渲染 PNG/JPEG 后视觉检查）
- [ ] PDF 成功、页渲染失败、超页和损坏时均关闭 document、page、bitmap 和文件句柄。（验证：资源 spy 与 Windows 文件可删除检查）
- [ ] 加密、空 PDF、超页 PDF 和损坏 PDF 返回明确错误码。（验证：逐个上传固定夹具）
- [ ] 上传拒绝扩展名伪装、MIME 不符、错误文件签名和超大小文件。（验证：multipart 集成测试）
- [ ] 混合批量上传中单文件失败不会删除其他成功文件。（验证：一个合法和一个非法文件同时上传）
- [ ] 文件预览只能读取数据库登记且位于数据根目录内的文件。（验证：构造 `..`、绝对路径和符号链接越界）
- [ ] 上传失败后临时文件和未入库持久文件被清理。（验证：检查临时与上传目录）

## 百炼客户端与调用边界

- [x] 视觉客户端能解析字符串 message content。（验证：HTTPX MockTransport 固定响应）
- [x] 视觉客户端能按顺序拼接文本块数组 content。（验证：返回多个 text block）
- [ ] 空 content、非 JSON HTTP 响应、401/403、429、5xx、超时和网络错误得到不同错误码。（验证：逐项 MockTransport）
- [ ] 持续 429 或 5xx 的请求次数不超过配置上限。（验证：记录 fetch 次数）
- [ ] 请求快照不包含 Authorization、API Key、Base64 图像或完整环境变量。（验证：序列化快照并搜索唯一标记）
- [ ] 原生联网搜索请求启用 search、source 和 forced search。（验证：检查完整请求体）
- [ ] 只有同时存在直接答案、HTTP(S) 来源和足够置信度时才标记联网命中。（验证：分别缺少一个条件）
- [ ] `javascript:`、`data:` 和无协议来源被丢弃。（验证：恶意 search results 夹具）
- [ ] 联网搜索请求不包含任务名、班级、教师、学生或学生答卷。（验证：检查假服务收到的请求体）
- [ ] 模型生成保存答案、评分点、依据、置信度、原始响应和 Token 用量。（验证：查询运行详情）

## JSON 提取与逐题容错

- [x] 纯 JSON 对象可以提取。（验证：parser 单元测试）
- [x] Markdown `json` 代码块可以提取。（验证：前后空白和大小写 fence）
- [x] JSON 前后带模型说明文字时可以提取。（验证：raw_decode 候选扫描）
- [x] 根节点为题目数组时被转换为统一 paper 结构。（验证：数组夹具）
- [x] 多个 JSON 候选时选择含题目结构的候选。（验证：前置元数据对象加试卷对象）
- [x] 字符串中含大括号或方括号时不会错误截断。（验证：题干含公式和括号）
- [x] Python 表达式、函数调用和恶意对象文本不会被执行。（验证：带副作用表达式后检查文件和环境不变）
- [ ] `questions`、约定中文根字段和题目字段别名均能映射。（验证：参数化别名测试）
- [ ] 数字/字符串题号、数字字符串满分和布尔字符串均能转换。（验证：混合类型夹具）
- [ ] 初中数学和高中物理的中文/英文题型别名正确映射。（验证：参数化双科目题型测试）
- [ ] 缺失或重复题号生成唯一待核号，并设置 requires correction。（验证：检查草稿字段和 issue）
- [ ] 未知题型映射为当前科目兜底题型并要求教师修正。（验证：双科目未知值）
- [ ] 缺省置信度为 0，越界置信度被夹取并标记关注。（验证：缺失、负数和大于 1）
- [ ] 非对象题目、缺题干和非法满分只形成局部 skipped/blocking issue。（验证：合法与非法节点混合）
- [x] 只要存在可用题目，任务进入教师审核而不是整卷失败。（验证：混合夹具执行完整提取）
- [x] 无参考答案模式在最终 schema 校验前清空模型提前返回的答案和评分点。（验证：本次真实失败脱敏夹具）
- [x] 本次夹具第 5–8 题不再触发整卷 `ANSWER_EXTRACTION_INVALID`。（验证：断言全部可用题目进入草稿）
- [ ] 被清除的提前答案/评分点记录为非阻塞 issue，不伪装为搜索结果。（验证：检查 parsed output 与草稿 source）
- [ ] 参考答案模式丢弃空描述、非数字、非有限和负分评分点。（验证：混合非法评分点）
- [x] 参考答案超分评分点按 Decimal 等比例缩放，合计精确不超过满分。（验证：小数与多项评分点）
- [x] 评分点调整保存原值、调整值和原因，并标记需教师关注。（验证：API 和 UI 对照）
- [x] 本地解析完全无可用题目时恰好发起一次 structure repair。（验证：假模型调用计数）
- [ ] structure repair 只发送原始模型文本、科目、模式和目标 schema。（验证：请求中不存在图片、文件路径和个人信息）
- [ ] 修复成功后进入审核；修复失败后任务进入可重试失败。（验证：两类固定响应）
- [ ] 修复失败运行历史恰好包含原识别与一次修复，不出现递归调用。（验证：数据库查询）

## 答案配置、审核与版本

- [ ] 创建任务支持双科目和两种答案方式，API contract 与 React 保持一致。（验证：创建四种组合）
- [ ] 参考答案模式缺文件被拒绝；Agent 模式上传参考答案被拒绝。（验证：直接 multipart 请求）
- [ ] 首次答案配置创建 V1 并显示识别、搜索、生成和待审核状态。（验证：延迟假模型逐阶段轮询）
- [ ] 局部解析问题显示位置、原因、原值和归一化结果。（验证：打开答案审核页）
- [ ] requires correction 题目不能直接审核通过。（验证：调用 approve API）
- [ ] 教师提交完整有效修改后 requires correction 被清除。（验证：修改题号/题型/满分/答案/评分点）
- [ ] 版本级 blocking issue 阻止发布并提示重新识别。（验证：保留 skipped 节点调用发布 API）
- [ ] 失败或有版本级阻塞的整卷重试创建新版本并保留旧运行。（验证：比较版本号和历史）
- [ ] 旧草稿版本被 supersede，不产生重复正式题目。（验证：查询版本与 questions）
- [ ] 搜索命中显示来源；未命中自动生成并显示模型生成。（验证：混合两题任务）
- [ ] 单题搜索/生成失败不影响其他题进入审核。（验证：五题中固定一题失败）
- [ ] 单题 research 只增加该题搜索/生成运行。（验证：比较各题运行数）
- [ ] 单题 regenerate 跳过搜索并保留旧运行。（验证：检查调用顺序和历史）
- [ ] 教师修改、通过、退回、重搜、重生成和发布均写入审计。（验证：查询事件类型和时间）
- [ ] 所有题审核通过且无阻塞问题后可以发布。（验证：完整审核流程）
- [ ] 发布事务失败时题目、版本状态、活动版本和审计全部回滚。（验证：注入 question insert 失败）
- [ ] 已发布版本只读，修订创建 V2 且 V1 正式题不变。（验证：比较两版本）
- [ ] 存在新草稿版本时服务端阻止新学生上传和新批改。（验证：绕过前端直接调用 API）

## 学生批改、教师终审与报告

- [ ] 学生提交创建时绑定当前批准答案版本。（验证：V1/V2 分别上传学生）
- [ ] 后续发布新版本不会改变旧提交绑定。（验证：重新查询旧提交）
- [ ] 学生批改只读取提交绑定版本，不读取当前草稿或新版本。（验证：设置不同答案后批改）
- [ ] 初中数学选择、填空、简单简答题完成模型初评。（验证：固定三题输入）
- [ ] 高中物理选择、填空、计算题完成模型初评。（验证：计算题含公式、代入、结果和单位评分点）
- [ ] 模型原始响应、逐题识别答案、模型分和评分理由完整保存。（验证：查看运行与复核详情）
- [ ] 低置信度、缺题和越界分数进入需人工判断。（验证：固定异常输出）
- [ ] 教师可以修改最终答案、分数和批注。（验证：保存后刷新）
- [ ] 未全部复核时整卷确认被拒绝。（验证：保留一道 pending）
- [ ] 整卷确认后报告使用教师最终分和确认人。（验证：模型分与教师分不同）
- [ ] 修改已确认成绩会撤销确认并移出统计。（验证：确认后重新改分）
- [ ] 旧答案版本报告在新版本发布后保持可读。（验证：V1 学生报告）
- [ ] 班级统计只包含已确认提交并按答案版本正确分组。（验证：混合 confirmed/pending/V1/V2）
- [ ] 批量学生批改并发峰值不超过配置。（验证：假模型记录 active 数）
- [ ] 单提交失败不影响同批其他提交。（验证：一个固定模型错误）

## API 与 React 兼容

- [x] 所有既有 API 路径、HTTP 方法和主要状态码均由 FastAPI 提供。（验证：路由清单 contract 测试）
- [x] 成功响应保持 `{ok:true,data}`，错误响应保持 `{ok:false,error}`。（验证：成功、验证、冲突、404、500）
- [ ] Pydantic 验证错误转换为现有 fields 结构。（验证：任务、草稿和复核非法请求）
- [ ] 未知异常不返回调用栈、绝对路径或原始数据库错误。（验证：注入 SQLite 异常）
- [ ] 任务列表、创建、答案审核、上传、复核和报告无需更改 URL 即可使用。（验证：Playwright 完整导航）
- [ ] 运行历史可区分 exam/reference extraction、structure repair、search 和 generation。（验证：打开历史抽屉）
- [ ] 题卡显示 requires correction、解析 issue 和评分点调整记录。（验证：组件测试）
- [ ] 页面对诊断状态使用文字/图标而不只依赖颜色。（验证：DOM 和无色截图）
- [ ] 未保存编辑离开或重新识别前收到提示。（验证：修改字段后导航）
- [ ] 创建、答案审核、上传、复核、报告和错误诊断页面在 1280px 无横向遮挡。（验证：截图并比较 scrollWidth/clientWidth）
- [ ] 浏览器控制台无未处理错误。（验证：全页面视觉脚本收集 console/pageerror）

## 恢复、并发与审计

- [ ] 服务启动把 running 视觉识别标记为可重试失败。（验证：预置运行后重启）
- [ ] 服务启动把 running structure repair 标记为可重试失败。（验证：预置运行后重启）
- [ ] 服务启动把 running search/generation 标记为可重试失败。（验证：分别预置）
- [ ] 服务启动把 processing 学生提交和 running model run 标记为中断失败。（验证：预置后重启）
- [ ] 恢复逻辑连续执行两次不会重复审计或改变已恢复状态。（验证：两次启动）
- [ ] 同一任务不能并发启动两次答案配置。（验证：同时发送两个请求）
- [ ] 同一草稿不能并发 research/regenerate。（验证：同时发送请求）
- [ ] 同一提交不能并发批改或重试。（验证：同时发送请求）
- [ ] worker 中一个任务异常不会终止其他 worker。（验证：队列混合成功/失败任务）
- [ ] 服务关闭后不再接收新任务，未完成任务被持久化为可恢复状态。（验证：运行中发送 shutdown）
- [ ] 审计覆盖上传、识别、结构修复、归一化、搜索、生成、教师审核、发布、批改和确认。（验证：完整流程查询事件）

## 安全与敏感信息

- [ ] 模型状态、API 响应和请求快照中不存在唯一测试 Key。（验证：递归搜索序列化输出）
- [ ] SQLite 所有文本/JSON 列中不存在唯一测试 Key。（验证：逐表逐列搜索）
- [ ] 服务日志中不存在 Key、Authorization、完整 Base64 或未截断原始响应。（验证：触发成功和错误后搜索日志）
- [ ] React 构建产物中不存在 Key 和服务端环境变量值。（验证：构建后递归搜索）
- [ ] 结构修复和联网搜索请求不包含个人信息。（验证：假服务完整请求捕获）
- [ ] 来源标题、摘要和解析错误作为纯文本显示，不能注入 HTML/脚本。（验证：恶意夹具 UI 测试）
- [ ] 原始模型响应和来源只有读取接口，没有修改接口。（验证：检查路由并尝试 PUT/PATCH）

## 编译、测试与交付

- [x] `ruff check` 通过。（验证：运行 Python lint）
- [x] `mypy` 通过且无忽略整个模块的配置。（验证：运行 Python typecheck）
- [x] Python 单元与集成测试全部通过。（验证：运行 pytest）
- [x] React TypeScript 类型检查通过。（验证：运行前端 typecheck）
- [x] React UI 测试全部通过。（验证：运行 Vitest UI 套件）
- [x] React 生产构建成功，Python `compileall` 成功。（验证：运行统一 build）
- [x] `npm test` 能执行 Python 和 React 全部默认测试且不发送真实百炼请求。（验证：使用无 Key 环境运行）
- [x] 测试结束后无遗留服务器、SQLite 连接、worker、任务或计时器。（验证：命令正常退出并检查进程）
- [x] 运行时依赖中不再包含 Express、Multer、Sharp 或服务端 PDF.js。（验证：检查 package lock 与运行入口）
- [x] 工作区不存在可运行的 Express 服务端入口。（验证：搜索 `express()` 和旧 start 脚本）
- [x] README 包含安装、配置、开发、测试、生产、备份、迁移、切换和回滚步骤。（验证：新环境按文档执行）
- [x] README 明确 Python 为唯一正式写服务，并提醒禁止新旧后端同时写 SQLite。（验证：切换章节审阅）
- [x] README 说明结构容错、评分点归一化和最多一次结构修复。（验证：与 spec 对照）

## 端到端场景

- [ ] 场景 1——现有数据迁移：备份 v2 数据 → 停止 Node → Python 迁移 → 打开旧任务、旧报告和统计，结果一致。
- [ ] 场景 2——本次故障回归：上传 7 页物理 PDF → 无参考答案识别 → 模型提前返回超额评分点 → 系统清除提前答案/评分点 → 全部题目进入搜索和教师审核。
- [ ] 场景 3——参考答案归一化：上传物理试卷和参考答案 → 某计算题评分点超分 → 系统归一化并显示调整记录 → 教师确认后发布。
- [ ] 场景 4——局部坏题：响应包含合法题、重复题号和非法节点 → 合法题保留、待核题可修改、遗漏被提示 → 修正/重识别后发布。
- [ ] 场景 5——结构修复：首次响应完全无题目 → 只调用一次修复 → 修复成功进入审核；再次运行修复失败夹具进入可重试失败。
- [ ] 场景 6——搜索与生成：高中物理无参考答案 → 一题联网命中带来源、一题模型回退 → 教师审核发布 → 学生批改。
- [ ] 场景 7——版本追溯：发布 V1 并确认学生 A → 修订 V2 时阻止新上传 → 发布 V2 并确认学生 B → 两份报告显示各自版本。
- [ ] 场景 8——故障恢复：识别或批改运行中停止服务 → Python 重启 → 状态明确失败 → 教师安全重试成功。
- [x] 场景 9——生产冒烟：生产 Python 进程同时提供健康、任务 API、React 页面和文件预览，浏览器无错误。
- [ ] 场景 10——真实百炼数学：教师显式确认费用后，用脱敏试卷完成有参考答案和无参考答案流程，保存运行 ID、模型、Token 和审核结果。
- [ ] 场景 11——真实百炼物理：教师显式确认费用后，用本次或另一脱敏物理试卷完成搜索、生成、归一化、发布和学生批改。
