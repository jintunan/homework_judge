# 试卷题目识别与参考答案匹配 Checklist

> 每一项都必须通过运行代码、观察页面或核对持久化结果验证。最终验收记录实际结果，不以“应该可以”代替证据。

## 上传与文件处理

- [ ] 同时选择试卷和答案并提交后创建一个任务，两份文件的角色、名称、大小和摘要正确保存。（验证：调用创建接口并查询任务详情；对应 AC1）
- [ ] 缺少任一文件时前端不提交，绕过前端直接调用接口也返回字段级错误。（验证：UI 测试 + API 测试）
- [ ] PDF、DOCX、JPG、PNG 作为试卷或答案均能通过合法文件校验。（验证：四种格式参数化测试；对应 AC2）
- [ ] 扩展名与实际内容不符、空文件、损坏文件、超大小和超页数均得到稳定错误码。（验证：上传拒绝测试；对应 AC2）
- [ ] 同名文件重复上传不会覆盖已有任务文件。（验证：比较两个任务的存储路径和 SHA-256）
- [ ] PDF 页面按原顺序渲染且页码从 1 连续递增。（验证：多页夹具渲染测试）
- [ ] DOCX 经转换后公式、选项和示意图在页面图像中可读。（验证：渲染真实样本并检查所有页面；对应 AC2）
- [ ] JPG 的 EXIF 方向和 PNG 的透明背景处理正确。（验证：旋转 JPG 与透明 PNG 夹具）
- [ ] 转换成功和失败后均不存在被占用的 PDF/DOCX 句柄或未清理的任务临时目录。（验证：测试后重命名原文件并检查临时目录）

## 试卷识别

- [ ] 试卷提示词只要求提取结构，不要求解题或生成答案。（验证：提示词快照）
- [ ] 识别结果包含题号、题干、选项、题型、分值、来源页码、置信度和问题列表。（验证：解析夹具；对应 AC3）
- [ ] 单选、多选、填空、计算、简答和未知题型能归一化到约定枚举。（验证：题型参数化测试）
- [ ] 分值缺失或不可解析时保留题目并生成明确异常，不虚构分值。（验证：坏分值夹具）
- [ ] 同一题跨页时来源页码完整，重叠批次不会生成重复题目。（验证：跨批次模拟响应）
- [ ] 同号但题干不同的题目不会被合并，并标记重复题号。（验证：冲突夹具）
- [ ] 一个坏题目节点不会导致同批其他合法题目丢失。（验证：混合节点夹具）
- [ ] 完全没有可用题目时试卷识别阶段失败，并显示运行 ID 和安全错误摘要。（验证：空响应集成测试）

## 答案识别

- [ ] 精简答案文件能提取题号、标准答案、解析和来源页码。（验证：精简答案夹具；对应 AC4）
- [ ] 完整解析版能额外提取题干提示并进入相同 AnswerEntry 结构。（验证：解析版夹具；对应 AC4）
- [ ] 只有答案、没有解析的合法条目被保留，解析为空但不阻塞匹配。（验证：无解析夹具）
- [ ] 缺少标准答案的条目被标记异常，不能作为自动确认结果。（验证：空答案夹具）
- [ ] 重叠页产生的答案条目正确去重，同号不同内容保留为冲突。（验证：答案跨批次测试）
- [ ] 模型输出纯 JSON、代码块、带说明文本、对象根和数组根均可解析。（验证：解析器参数化测试）
- [ ] 原始模型响应、归一化条目和局部解析问题分别保存。（验证：查询 run 和 review API）

## 题号规范化与匹配

- [ ] `1、`、`第１题` 和 `一` 在适用场景规范化为同一主题号。（验证：题号单元测试）
- [ ] `1（2）`、`1-2` 等子题格式保留层级，不与主题 `1` 混为一题。（验证：题号单元测试）
- [ ] 章节标题、选项字母和无法确定的编号不会被强行转换。（验证：非法输入测试）
- [ ] 两侧唯一规范化题号一致时建立 `number_exact` 建议并显示题号依据。（验证：匹配测试；对应 AC6）
- [ ] 题号缺失但题干高相似、第一候选领先足够时建立 `stem_similarity` 建议。（验证：匹配测试；对应 AC7）
- [ ] 匹配详情显示题号分、题干分、顺序分、总分和人类可读原因。（验证：review API；对应 AC7）
- [ ] 重复题号不自动匹配，所有相关条目进入冲突状态。（验证：重复号测试；对应 AC8）
- [ ] 两道题竞争一条答案且无明显领先者时不强行分配。（验证：竞争候选测试；对应 AC8）
- [ ] 一条答案最多被一道题使用，数据库和服务端均阻止重复占用。（验证：约束 + API 冲突测试）
- [ ] 没有候选的题目显示“缺少答案”，未使用答案显示在孤立答案区域。（验证：审核页集成测试）
- [ ] 使用同一批 QuestionDraft 和 AnswerEntry 连续匹配两次，结果与排序完全一致。（验证：确定性测试；对应 AC6）
- [ ] 自动匹配只产生建议或待处理状态，不直接产生教师已确认状态。（验证：匹配结果断言）

## 处理状态、失败与重试

- [ ] 页面准备、试卷识别、答案识别和匹配按顺序更新状态与进度。（验证：轮询集成测试；对应 AC5）
- [ ] 处理中重复点击不会创建第二个并行流水线或重复模型调用。（验证：并发启动测试）
- [ ] 401/403 不重试，429/5xx/临时网络错误只按配置有限重试。（验证：模型客户端测试）
- [ ] 失败响应显示阶段、错误码、摘要和重试入口，不显示调用栈或绝对路径。（验证：失败 UI + API；对应 AC5）
- [ ] 重试创建新 run，旧 run、原始响应、用量和失败信息仍可查询。（验证：运行历史；对应 AC12）
- [ ] 服务重启后 running 运行变成 interrupted，任务变为可重试失败状态。（验证：重启恢复测试；对应 AC14）
- [ ] 服务重启不会自动重新调用模型或产生不可见费用。（验证：模拟客户端调用计数）

## 审核工作台

- [ ] 审核页显示题目总数、已确认、待确认、冲突、缺答案和孤立答案数量。（验证：页面观察）
- [ ] 题目导航可按状态筛选并准确跳转。（验证：UI 测试）
- [ ] 当前题同时显示题干、选项、题型、分值、答案、解析和自动/有效值差异。（验证：页面观察；对应 AC9）
- [ ] 点击题目或答案来源页后，预览切换到正确文档和页码。（验证：UI 测试；对应 AC9）
- [ ] PDF、DOCX、JPG、PNG 的派生页面均可在统一预览器中查看。（验证：四种文档审核页）
- [ ] 修改题号、题干、选项、题型或分值后刷新页面，人工值仍存在且原始值仍可查看。（验证：API + 页面刷新；对应 AC10）
- [ ] 修改标准答案或解析后刷新页面，人工值仍存在且原始答案条目未被修改。（验证：API + 数据库；对应 AC10）
- [ ] 可以为题目重新选择未使用答案、解除匹配或直接填写答案。（验证：UI 操作；对应 AC10）
- [ ] 选择已被其他题占用的答案时显示冲突，不静默抢占。（验证：UI + API）
- [ ] 修改已确认题目的任何有效字段后，该题自动回到待确认。（验证：UI + API）
- [ ] 孤立答案可以被人工匹配或明确标记为无关，操作有审计记录。（验证：异常面板）
- [ ] 状态、冲突和低置信度同时有文字或图标表达，不只依赖颜色。（验证：无障碍检查）

## 完成准入

- [ ] 每道题均有非空题号、题干、合法题型和标准答案时才满足字段完整条件。（验证：逐字段缺失测试）
- [ ] 存在未匹配题目、阻塞问题、匹配冲突或未确认题目时，前端完成按钮不可用。（验证：UI 测试；对应 AC11）
- [ ] 绕过前端直接调用完成接口时，服务端返回每个阻塞原因并拒绝状态变更。（验证：API 测试；对应 AC11）
- [ ] 未处理的孤立答案阻止完成；标记为无关后不再阻止。（验证：准入测试）
- [ ] 全部题目确认且异常清理后，任务可进入 completed，刷新和重启后保持完成。（验证：端到端流程）

## 持久化、审计与安全

- [ ] 最近任务列表在刷新和服务重启后保持文件名、状态、题目数和更新时间。（验证：重启测试；对应 AC14）
- [ ] 原始题目、原始答案、自动匹配和人工覆盖分层保存，不互相覆盖。（验证：数据库查询）
- [ ] 上传、处理、失败、重试、编辑、重配、确认、重开、忽略和完成均有审计事件。（验证：审计查询）
- [ ] `DASHSCOPE_API_KEY` 只从服务端环境读取，不出现在前端环境变量或构建文件。（验证：构建产物搜索）
- [ ] 浏览器响应、SQLite、日志和运行快照均搜索不到测试 API Key。（验证：安全集成测试；对应 AC13）
- [ ] 请求快照不保存 Authorization、Base64 图片或任意绝对文件路径。（验证：run 记录检查）
- [ ] 文件预览通过数据库 ID 和安全相对路径解析，`..`、绝对路径和未知 ID 均被拒绝。（验证：路径穿越测试；对应 AC13）
- [ ] 错误响应不包含调用栈、内部异常 repr 或服务器绝对路径。（验证：故障注入测试）

## 编译、测试与运行

- [ ] Python 格式化和 Ruff 检查通过。（验证：运行项目 lint 命令）
- [ ] mypy 严格类型检查通过。（验证：运行项目 typecheck 命令）
- [ ] 后端单元与集成测试全部通过。（验证：运行 pytest；对应 AC15）
- [ ] 前端 TypeScript 类型检查和 UI 测试全部通过。（验证：运行 typecheck 与 Vitest；对应 AC15）
- [ ] 前端生产构建成功，构建结果中不存在 API Key 或本机绝对路径。（验证：build + 文本扫描）
- [ ] 单进程生产服务可同时提供健康接口、API 和 SPA，直接刷新审核子路由仍可打开。（验证：生产启动冒烟测试）
- [ ] 测试和服务退出后无遗留端口、后台任务、数据库锁或临时转换进程。（验证：进程与文件检查）
- [ ] 1280×720 桌面窗口中上传、进度和审核页无横向溢出、遮挡或不可见主操作。（验证：视觉检查脚本）

## 端到端场景

- [ ] 场景 1：上传一组“原卷版＋解析版”文件 → 页面转换 → 分别识别 → 自动匹配 → 人工处理异常 → 逐题确认 → 完成；所有题目和答案都有明确去向。（验证：真实百炼运行记录；对应 AC16）
- [ ] 场景 2：上传 `(14)静电力与电场强度.pdf` 与其答案 PDF → 识别精简答案 → 自动匹配 → 人工处理异常 → 完成；答案没有重复占用。（验证：真实百炼运行记录；对应 AC16）
- [ ] 场景 3：构造重复题号和低相似答案 → 系统保持待处理 → 人工重新指定答案 → 保存并确认。（验证：受控夹具；对应 AC8、AC10）
- [ ] 场景 4：在答案识别阶段模拟服务中断 → 重启后任务显示可重试 → 重试生成新运行且旧记录仍在。（验证：中断集成测试；对应 AC12、AC14）

## 最终验收报告

- [ ] `docs/acceptance-report.md` 记录总检查数、通过数、未通过项、实际命令输出摘要和修复结果。
- [ ] 两个真实样本场景记录模型 ID、运行 ID、页数、题目数、答案条目数、自动匹配数、人工处理数和 Token 用量。
- [ ] 所有未通过项均已修复并重新验证，或明确说明阻塞原因并获得用户接受。

## 增量验收：公式显示与无门槛编辑

### 依赖与离线资源

- [x] KaTeX、MathLive 使用明确固定版本，锁文件已更新，未引入 CDN 地址。（验证：核对 `package.json`、锁文件和构建产物；对应 N11）
- [x] MathLive 字体及必要样式被 Vite 打入本地静态资源。（验证：检查构建清单和浏览器 Network；对应 N11）
- [ ] 浏览器离线模式刷新审核页后，公式显示和可视化输入仍可正常使用。（验证：Chrome/Edge 离线刷新；对应 AC23）

### 公式解析与序列化

- [x] `$...$`、`$$...$$`、`\(...\)`、`\[...\]` 均被正确识别为行内或独立公式。（验证：解析参数化测试；对应 F20）
- [x] 中文、公式、标点、空格和换行的顺序在解析后保持不变。（验证：混排夹具；对应 AC18）
- [x] 转义美元符号和普通货币文本不被误判为公式。（验证：边界输入测试）
- [x] 同一字段包含多个公式及相邻公式时，拆分顺序和显示类型正确。（验证：解析单元测试）
- [x] 未闭合、空内容和无法解析的公式被标记为异常片段，解析函数不抛出未处理异常。（验证：坏输入测试；对应 AC21）
- [x] 历史公式保留原分隔符，新插入公式按行内或独立类型使用规范分隔符。（验证：序列化断言）
- [x] 同一内容连续执行三次“解析—序列化”后字符串稳定，不增加反斜杠或分隔符。（验证：往返测试；对应 AC20）

### 阅读视图

- [x] 题干、所有选项、标准答案和解析中的合法公式均显示为数学排版，不把分隔符和 LaTeX 命令作为普通正文暴露。（验证：审核页组件测试；对应 AC17）
- [x] 分数、根号、上下标、希腊字母、矢量、积分、求和、极限和箭头正确显示。（验证：公式结构夹具；对应 AC18）
- [x] 行内公式与中文基线协调，独立公式单独排版，长公式可滚动且不撑破中间栏。（验证：1280×720 页面观察；对应 N15）
- [x] 单个公式无效时显示完整原文和“公式需检查”，同一字段的其他内容继续正常显示。（验证：混合好坏公式测试；对应 AC21）
- [x] `<script>`、事件属性、HTML 标签、链接和远程图片命令均不执行、不加载外部资源。（验证：安全夹具和 Network 检查；对应 AC23）

### 可视化公式编辑

- [x] 点击已有公式或键盘聚焦后按 Enter，可以打开公式编辑面板，界面不显示 LaTeX 源码。（验证：交互测试；对应 F21）
- [ ] 教师可用普通键盘输入数字、字母和运算符，并用屏幕键盘选择常用数学符号。（验证：浏览器操作；对应 F22）
- [x] 分数、根号、上下标、括号、绝对值、希腊字母、求和、积分、极限、矢量和箭头均有可发现的输入入口。（验证：工具栏逐项操作；对应 F22）
- [ ] 公式面板支持确认、取消、撤销和重做；取消后公式值不变，确认后阅读预览立即更新。（验证：交互测试；对应 F23）
- [ ] 仅使用键盘能够打开公式、选择常用结构、确认或取消，并在关闭后返回原编辑位置。（验证：键盘验收；对应 AC25）
- [ ] MathLive 初始化或字体异常时显示可理解错误，原公式字符串和页面其他操作不受影响。（验证：故障注入）

### 文字与公式混合编辑

- [x] 普通文字可直接输入、删除、换行和粘贴，教师不需要接触 LaTeX。（验证：混合编辑测试；对应 AC19）
- [ ] 可在当前光标位置插入行内公式和独立公式，插入后文字与公式顺序正确。（验证：光标交互测试；对应 F23）
- [ ] 公式作为完整对象被选择和删除，不会因普通文字输入而拆坏内部结构。（验证：Backspace/Delete 测试）
- [ ] 从网页粘贴的内容按纯文本处理，不带入标签、脚本、事件和外部样式。（验证：富文本粘贴测试；对应 N12）
- [x] “取消编辑”恢复进入本字段编辑前的完整内容；“完成编辑”只更新页面本地状态，未点击“保存修改”前不写后端。（验证：API 调用计数）
- [ ] 保存失败时保留本地文字和公式修改，并显示错误；不会静默恢复旧内容。（验证：模拟 PATCH 失败）
- [x] 保存并刷新后文字、公式、标点和换行保持一致，连续三次编辑保存不损坏。（验证：浏览器 + API 往返；对应 AC20）

### 审核页集成与权限

- [x] 题干、选项、答案和解析全部使用统一公式显示与编辑行为，题号、题型和分值输入保持原状。（验证：ReviewPage 测试；对应 F19）
- [x] 关联答案条目时标准答案和解析只读但公式正常显示；解除或更换关联后编辑权限恢复。（验证：关联状态测试；对应 AC22）
- [ ] 页面现有“保存修改”“确认本题”“取消确认”“上一题”“下一题”和“完成任务”逻辑无回归。（验证：审核页回归测试）
- [x] 在 1280×720 下页面可滚动，公式面板和虚拟键盘不遮挡底部固定操作栏，所有主按钮可达。（验证：Chrome/Edge 视觉检查；对应 AC24）
- [ ] 公式错误使用文字提示和可识别名称，不只依赖颜色；编辑入口和工具栏具有可访问名称。（验证：Testing Library + 键盘检查；对应 N16）

### 自动化与真实任务

- [x] `pnpm typecheck` 通过。（验证：命令退出码 0）
- [x] `pnpm test:ui` 通过，包含解析、阅读、编辑和审核页回归测试。（验证：命令退出码 0；对应 N17）
- [x] `pnpm test` 通过，后端现有测试无回归。（验证：命令退出码 0）
- [x] `pnpm build` 成功，构建产物不包含本机绝对路径或外部公式资源 URL。（验证：构建与文本扫描）
- [ ] 真实任务第 1 题及第 10～15 题的题干、选项、答案和解析公式逐项核对通过。（验证：与右侧原卷/答案页面对照；对应 AC17-AC18）
- [ ] Chrome 和 Edge 均完成阅读、编辑、保存、刷新、切题和离线验收。（验证：浏览器记录；对应 AC23-AC25）

## 增量验收：作业批改 Agent

> 本节所有条目都以已经确认题目、标准答案和题型的同版学生试卷为输入。正式模型准确率与自动通过阈值只使用教师确认标签验收。

### 学生试卷接收、对齐与作答切分

- [ ] 上传与原卷同版且页面完整的学生试卷后，系统创建学生提交并显示学生标识、文件名和处理状态。（验证：API 集成测试和学生列表观察；对应 AC26）
- [ ] 缺页、重复页、严重模糊或无法对应的页面被明确阻断或转人工处理，不产生静默最终分。（验证：四类异常夹具；对应 AC26）
- [ ] 同一学生页面与同一原卷重复对齐，题目区域映射结果在允许误差内稳定。（验证：重复运行并比较坐标；对应 AC27）
- [ ] 页面对齐低于阈值时不会继续绘制红圈或勾，而是显示对齐问题和人工处理状态。（验证：低特征页面夹具；对应 AC27）
- [ ] 每道题具有独立作答证据，填空题拆为独立空位，多区域或跨页计算题按页面与顺序聚合。（验证：区域查询和预览；对应 AC28）
- [ ] 每题识别结果包含内容、来源区域、置信信息和异常；故意使一题失败后，其他题结果仍被保存。（验证：故障注入；对应 AC29）
- [ ] 单选、多选、填空和计算题分别进入已确认题型对应的批改器，模型不能改变题型。（验证：路由器调用桩；对应 AC30）
- [ ] 未确认题型或不支持题型明确阻断正式评分，不被模型猜测为其他题型。（验证：非法题型 API 场景；对应 AC30）
- [ ] 原始学生试卷的文件摘要在识别、评分、复核、批注和报告生成前后完全一致。（验证：运行前后比较 SHA-256）

### 评分配置与计算题评分细则

- [ ] 教师可配置题目满分、填空数量、各空独立分值、答案类型和同义答案。（验证：配置 API 与页面往返）
- [ ] 计算题评分细则草案可由教师新增、删除、修改和排序评分点，也可调整依赖关系。（验证：评分细则编辑器操作；对应 AC31）
- [ ] 评分点标识重复、依赖不存在、依赖成环、分值非法或分值和不等于满分时，版本不能冻结。（验证：参数化 API 测试；对应 AC31）
- [ ] 合法评分细则冻结后可以用于评分，并记录确认人、冻结时间和内容摘要。（验证：数据库与 API 查询；对应 AC31）
- [ ] 冻结版本不能原地修改；继续编辑会创建新版本，旧版本和旧评分结果仍可查看。（验证：版本历史场景；对应 AC32）
- [ ] 新评分细则版本不会静默改变旧运行，受影响提交被明确提示需要创建新评分运行。（验证：冻结新版本后查询历史；对应 AC32）
- [ ] 标准答案未确认或计算题没有冻结评分细则时不能启动正式评分。（验证：预检查 API 测试）

### 单选与多选评分

- [ ] 单选题完全匹配得满分，错误选项、多个选项和空白均得零分。（验证：参数化单元测试；对应 AC33）
- [ ] 正确选项为 `ACD`、满分 6 分时，`ACD`、`AC`、`A`、`AB` 和空白分别得到 `6.00`、`4.00`、`2.00`、`0.00` 和 `0.00`。（验证：评分单元测试；对应 AC34）
- [ ] 多选题只要包含一个错误选项就得零分，不因同时选中其他正确选项获得部分分。（验证：错选组合测试；对应 AC34）
- [ ] 不能整除的多选比例使用十进制计算并四舍五入到两位小数，同时保存原始比例与未舍入分数。（验证：数据库结果断言；对应 AC35）
- [ ] 相同输入、满分和规则重复计算得到字节级一致的比例与最终分数。（验证：重复执行测试；对应 AC58）

### 填空题评分

- [ ] 多空填空逐空保存位置、独立满分、识别答案、状态和得分，题目分数等于各空最终分数之和。（验证：多空夹具；对应 AC36）
- [ ] 单个空只出现满分、零分或待复核，不产生空内部分分。（验证：结果枚举和分数断言；对应 AC36）
- [ ] 规范化后完全一致或命中教师配置同义答案时由规则直接判定，且不调用大模型。（验证：模型调用计数；对应 AC37）
- [ ] 所有规范化后非完全一致答案都会调用大模型，并保存结构化结论、依据、原因和版本信息。（验证：模型桩调用计数与事件查询；对应 AC37）
- [ ] 科学计数法和可换算单位调用数值验证器；数学公式调用公式等价验证器。（验证：工具调用桩；对应 AC38）
- [ ] 数值或公式验证器与模型结论冲突时不能自动完成，必须生成 `MODEL_TOOL_CONFLICT` 复核项。（验证：冲突夹具；对应 AC38）
- [ ] 未知单位、定义域不明确、公式解析超时或模型无法判断时保留证据并进入复核，不猜测答案。（验证：边界输入测试）

### 计算题评分与严格依赖

- [ ] 每个计算题评分点都产生直接状态、分值、学生证据、简短原因和置信信息。（验证：逐题详情接口；对应 AC39）
- [ ] 自动得分评分点缺少可定位证据时被审计拦截，不能进入最终成绩。（验证：删除证据故障注入；对应 AC39）
- [ ] 模型新增评分点、修改分值或直接返回题目总分时，相应字段被拒绝或忽略，最终分仍由确定性代码计算。（验证：恶意模型输出桩）
- [ ] 构造 `P1 → P2 → P3` 和独立 `P4`，P1 错误后 P1-P3 均为零，P4 仍按自身作答计分。（验证：依赖传播测试；对应 AC40）
- [ ] `blocked_by_dependency` 结果记录导致失效的失败祖先，且不会被模型直接标记为得分。（验证：评分点结果查询；对应 AC40）
- [ ] 分项得分之和等于题目得分，题目得分处于零和满分之间，总分等于各题最终分之和。（验证：题级与整卷审计；对应 AC41）
- [ ] 评分细则未覆盖的新解法进入教师复核，系统不会临时修改冻结评分细则。（验证：新解法模型桩）

### 评分审计与教师复核

- [ ] 低识别置信度、模型无法判断、模型工具冲突、证据缺失、错误位置不确定、新解法未覆盖、依赖矛盾、分数矛盾和非法模型输出均能触发对应复核原因。（验证：原因参数化测试；对应 AC42）
- [ ] 高置信度、证据完整且无冲突的普通结果不会被默认送教师复核。（验证：无风险整卷场景；对应 AC42）
- [ ] 复核页同时展示题目、标准答案、评分细则、学生原图、识别结果、自动评分、证据、工具结论和触发原因。（验证：UI 集成测试；对应 AC43）
- [ ] 教师可确认自动结论，或修改识别、逐空判断、评分点直接状态、错误原因和错误位置。（验证：逐类复核操作；对应 AC43）
- [ ] 教师不能直接保存与分项不一致的题目总分，也不能在当前运行内修改冻结评分细则。（验证：前端禁用状态和绕过前端 API 测试）
- [ ] 教师修改后重新执行依赖传播、题级审计和整卷审计；仍有矛盾时不能完成。（验证：修改后故意保留矛盾；对应 AC44）
- [ ] 最后一个复核项解决且审计通过后，运行继续最终化；仍有开放项时保持 `needs_review`。（验证：状态转换集成测试）

### 试卷批注

- [ ] 满分题生成绿色勾，零分题生成红圈，部分分题生成橙色三角及“得分/满分”，待复核题不生成最终正误标记。（验证：标记映射测试；对应 AC45）
- [ ] 批注同时使用形状或文字表达结论，不只依赖绿色、红色和橙色。（验证：关闭颜色或灰度预览；对应 AC45、AC59）
- [ ] 红圈定位到学生实际错误答案、遗漏位置或计算题首个错误步骤；依赖连带失分不重复绘制大量错误圈。（验证：证据与标记引用比较；对应 AC46）
- [ ] 错误位置坐标不足时进入复核，不生成猜测性红圈。（验证：缺失坐标夹具；对应 AC46）
- [ ] 批注 PDF 页数、页序和页面尺寸与学生试卷一致，主要题干和学生答案没有被标记遮挡。（验证：PDF 渲染与几何碰撞检查；对应 AC47）
- [ ] 网页 SVG 预览与下载 PDF 使用同一批注几何，标记位置在允许误差内一致。（验证：比较 `marks.json` 与渲染结果；对应 AC47）
- [ ] 教师修改评分或错误位置后旧批注立即标记过期，新批注反映最新结果修订，原始试卷不变。（验证：修订与文件摘要查询；对应 AC48）

### 学生错题分析

- [ ] 每道非满分题包含题号、题型、得分、满分、真实错误裁剪图、错误原因、知识点和简短建议。（验证：报告 JSON 与 PDF；对应 AC49）
- [ ] 报告中的得分、错误位置和错误原因与最终评分结果及最终证据完全一致。（验证：跨表与报告对比；对应 AC49）
- [ ] 部分分计算题区分已掌握部分、首个真实失分点以及受依赖影响的后续评分点。（验证：依赖场景报告；对应 AC50）
- [ ] 错题报告不包含完整标准答案或完整解题过程，模型生成越界内容时被校验拒绝。（验证：越界反馈模型桩；对应 AC50）
- [ ] 满分题默认不进入逐题错题列表，但总分、题型得分和正确题数汇总正确。（验证：混合正误报告）
- [ ] 教师可以预览并下载当前批注试卷和错题报告，过期版本不会被默认当作当前结果。（验证：生成物页面与下载接口；对应 AC51）
- [ ] 教师修改最终结果后，旧报告过期，新报告使用与新批注相同的 `result_revision`。（验证：生成物版本查询）

### 状态、历史、恢复与幂等

- [ ] 运行可观察上传、页面校验与对齐、切分、识别、评分、审计、等待复核、生成批注、生成报告和完成状态。（验证：阶段事件查询与进度组件；对应 AC52）
- [ ] 等待复核时界面显示具体题目、分项和原因，处理完成后状态继续推进。（验证：复核端到端场景；对应 AC52）
- [ ] 同一提交重新评分创建新运行，旧运行的输入快照、评分细则、模型结果、人工修改和生成物仍可查看。（验证：历史运行查询；对应 AC53）
- [ ] 模型超时或临时失败按配置有限重试；成功后不产生重复评分项，耗尽后保留已完成题目和可恢复状态。（验证：超时故障注入；对应 AC54）
- [ ] 服务在评分中途终止并重启后能从持久化阶段恢复，输入未变化的完成题目不重复调用模型或计分。（验证：真实重启测试；对应 AC55）
- [ ] 批注或报告生成失败只重试相应生成阶段，不重新执行题型批改器。（验证：生成器故障注入）
- [ ] 相同幂等键重复提交不会创建重复题目结果、复核项或当前生成物。（验证：重复请求与数据库计数）

### 安全、结构校验与确定性

- [ ] 日志、浏览器响应、数据库事件和导出文件中不出现测试 API Key。（验证：全目录与捕获日志搜索；对应 AC56）
- [ ] 外部模型请求只包含当前题目需要的题干、标准答案、评分细则和作答证据，不包含无关学生身份或整份原始试卷。（验证：模型请求桩检查；对应 AC56）
- [ ] 生成物下载使用数据库 ID 和安全相对路径，路径穿越、绝对路径和其他运行文件均被拒绝。（验证：下载安全测试）
- [ ] 大模型返回缺字段、非法枚举、越界分数或不存在的评分点/证据引用时，结果被拒绝并重试或转复核。（验证：结构化坏输出参数化测试；对应 AC57）
- [ ] 相同输入、冻结规则和工具版本重复执行选项评分、舍入、逐空求和和依赖传播，结果完全一致。（验证：重复执行并比较规范化 JSON；对应 AC58）
- [ ] 大模型自报置信度不能单独使高风险结果自动通过。（验证：高置信但证据缺失/工具冲突模型桩）
- [ ] 模型、提示词、结构 schema、评分细则和工具版本可以从任一最终题目结果追溯。（验证：运行详情和事件查询）

### 界面、可观测性与正式评测

- [ ] 最新版 Chrome 和 Edge 在 1280×720 下可以完成上传、查看进度、教师复核、预览和下载，页面无阻断性横向溢出。（验证：两浏览器人工记录；对应 AC59）
- [ ] 批注图层、筛选器、复核抽屉和下载按钮具有可识别文字或无障碍名称，可仅使用键盘完成主要复核操作。（验证：Testing Library 与键盘操作；对应 AC59）
- [ ] 可以按学生、题目和阶段查询耗时、模型用量、重试次数、失败类型和复核触发原因。（验证：事件与指标查询；对应 AC60）
- [ ] 一次最终评分使用的模型、提示词、工具和评分细则版本均可定位。（验证：从运行详情追溯；对应 AC60）
- [ ] 确定性评分规则、两位小数、严格依赖、审计、状态转换和生成物失效均有自动化测试。（验证：测试清单与覆盖查询；对应 AC61）
- [ ] 端到端自动化覆盖满分、部分分、错选、多空、非完全一致填空、依赖扣分、低置信度复核和教师修改重生。（验证：集成测试报告；对应 AC61）
- [ ] 现有合成标签明确标记为未确认，在教师确认前不会显示为正式准确率或用于自动通过阈值。（验证：基准元数据和配置；对应 AC62）
- [ ] 教师确认后的基准集分别报告对齐、识别、四类评分、复核召回和错误位置指标，并保留独立留出集。（验证：评测报告；对应 AC62）
- [ ] 模型、提示词或验证器版本改变后，旧阈值不会被静默沿用，系统要求重新校准或采用保守复核策略。（验证：版本变化场景）

### 模块集成与架构约束

- [ ] 学生试卷识别结果能够直接成为评分运行输入，评分结果不会覆盖 `student_responses` 原始识别记录。（验证：运行前后数据库对比）
- [ ] 题型路由、分数计算、依赖传播和审计不依赖大模型自由决策。（验证：关闭模型后客观题规则测试仍通过）
- [ ] 非完全一致填空和计算题模型判断均经过结构校验与确定性审计后才进入结果。（验证：调用链集成测试）
- [ ] 批注与报告模块只能读取最终结果，不能修改任何题目、分项或总分。（验证：生成前后评分表摘要一致）
- [ ] 功能开关关闭时，批改入口不可用，但现有试卷识别、答案匹配和学生作答识别流程无回归。（验证：关闭开关运行现有集成测试）
- [ ] 所有新增公开 API 至少有一个真实前端调用方或集成测试调用方。（验证：路由清单与调用搜索）
- [ ] 数据库从当前 schema 升级后旧任务、学生提交、页面和识别结果仍可读取。（验证：现有数据库副本迁移测试）
- [ ] 冻结评分细则和历史评分运行保持不可变，新版本通过显式引用关联。（验证：直接数据库与 API 更新尝试）

### 编译、测试与构建

- [x] 后端所有单元与集成测试通过。（验证：105 项通过，`pytest` 退出码为 0）
- [x] 前端所有 UI 测试通过。（验证：18 项通过，`vitest` 退出码为 0）
- [x] TypeScript 类型检查通过且批改契约无 `any` 逃逸。（验证：`pnpm typecheck` 退出码为 0并检查新增契约）
- [x] Ruff 与 mypy 严格检查通过。（验证：`pnpm lint` 退出码为 0）
- [x] 生产构建和 Python 编译检查通过。（验证：`pnpm build` 退出码为 0）
- [ ] 构建产物不包含 API Key、本机绝对路径、外部字体 URL或未授权远程资源。（验证：构建产物文本扫描）
- [ ] 测试和应用退出后没有遗留评分任务、模型调用进程、数据库锁或临时 PDF 进程。（验证：进程与文件句柄检查）

### 端到端场景

- [ ] 场景 A：上传一份同版、全部正确的学生试卷 → 自动对齐、识别和评分 → 无需复核 → 生成绿色勾批注试卷与空错题列表报告。（验证：完整 UI 与 API 运行）
- [ ] 场景 B：学生多选题分别出现全对、少选和错选 → 得到满分、比例部分分和零分 → 页面出现规定的勾、橙色三角和红圈。（验证：固定答案夹具）
- [ ] 场景 C：多空填空包含精确答案、教师同义答案、数值换算、公式等价和工具冲突 → 前四类正确计分，冲突项进入教师复核。（验证：模型与工具桩）
- [ ] 场景 D：计算题 P1 错误、P2/P3 依赖 P1、P4 独立 → 严格依赖扣分 → 只圈首个错误 → 报告区分真实错误与连带失分。（验证：冻结评分细则夹具）
- [ ] 场景 E：教师修正识别结果和错误位置 → 系统重新计分审计 → 旧批注和报告过期 → 新下载文件反映最新修订。（验证：复核工作台完整流程）
- [ ] 场景 F：评分中途停止服务 → 重启并安全恢复 → 已完成题目不重复调用模型 → 最终结果与无中断运行一致。（验证：真实进程重启）
- [ ] 场景 G：上传缺页或无法对齐试卷 → 系统停止自动评分并给出可操作原因 → 不生成最终正误标记。（验证：异常试卷夹具）

### 增量最终验收报告

- [ ] `docs/grading-agent-acceptance-report.md` 记录检查总数、通过数、未通过项、实际命令输出摘要、浏览器截图和生成物样本。（验证：审阅报告附件与命令记录）
- [ ] 验收报告记录应用版本、数据库 schema、模型、提示词、验证器、评分细则和基准数据版本。（验证：对照运行详情和依赖锁文件）
- [ ] 每个 AC26-AC62 至少关联一个实际通过证据，而不是只记录预期结果。（验证：检查验收标准到报告证据的映射表）
- [ ] 所有未通过项均已修复并重新验证，或明确记录阻塞原因、影响范围和用户接受结论。（验证：检查报告未通过章节和复验记录）

## 增量 Checklist：非重叠识别、边界合并与重复题生命周期

> 本节每一项必须通过自动化测试、数据库查询或页面观察验证；边界模型场景优先使用固定模型响应桩，真实模型运行作为端到端补充。

### 非重叠主识别与边界调度

- [ ] 试卷页数超过批次大小时，每个页码在主识别记录中恰好出现一次，批次页码按原顺序且无交集。（验证：运行非重叠批次参数化测试并检查 `main_batch` 记录；AC63）
- [ ] 答案页数超过批次大小时，每个页码在主识别记录中恰好出现一次，批次页码按原顺序且无交集。（验证：运行答案批次参数化测试；AC63）
- [ ] 三个试卷主批次产生两个边界调用，边界页分别是相邻批次的“末页＋首页”。（验证：模型桩调用参数断言；AC64）
- [ ] 三个答案主批次产生两个边界调用，不调用非相邻页面，也不重复调用同一边界。（验证：模型桩调用计数及页码断言；AC64、N46）
- [ ] 边界按照页码从左到右执行，前一边界合并后的草稿可以被下一边界引用。（验证：跨两个边界的连续内容单元测试）

### 试卷与答案跨页合并

- [ ] 跨两页题目合并后只有一个有效题目，包含两页可见的题号、题干、选项、题型、分值和来源页。（验证：试卷边界固定响应测试；AC65）
- [ ] 跨页题目的答题区域和整题区域完整合并、按页码排序且无重复区域。（验证：比较合并前后区域坐标；AC65）
- [ ] 跨页答案解析合并后只有一个有效答案条目，答案、解析和题干提示按原页顺序衔接。（验证：答案边界固定响应测试；AC66）
- [ ] 相同答案或解析片段不会因边界合并重复拼接。（验证：包含重复首尾文本的答案夹具；AC66）
- [ ] 边界合并提示词明确禁止解题、改写、概括和补造页面不可见内容。（验证：提示词快照测试；F85）

### 校验、降级与可观测性

- [ ] 识别运行可同时查询 `main_batch` 和 `boundary_merge` 记录，并包含角色、索引、页码、原始响应和解析问题。（验证：查询阶段运行 `raw_response_json`；AC67、N48）
- [ ] 运行记录保存模型 ID、提示词版本及主识别和边界调用汇总 Token 用量。（验证：模型桩用量累加断言；AC67）
- [ ] 边界模型超时或临时错误时，主识别草稿保留，其他边界继续处理，错误摘要不包含密钥或页面数据。（验证：故障注入测试；AC68、N52）
- [ ] 非法 JSON、缺失字段、非法 relation、错误页码、伪造草稿 ID、题号冲突和低置信度均被拒绝合并。（验证：边界校验参数化测试；AC68、N47）
- [ ] `uncertain` 和校验失败的相关草稿带有明确的跨页待审核异常，`separate` 不产生伪错误。（验证：比较三种决策输出；AC68）
- [ ] 异常边界合并结果不会进入最终匹配输入，合法主识别条目仍可继续匹配。（验证：识别—匹配集成测试；AC68）

### 最终确定性整理

- [ ] 带与不带“10.”题号前缀、LaTeX 与 Unicode 科学计数法、不同标点的同一道题只保留一条。（验证：真实重复场景回归夹具；AC69）
- [ ] 两个题号相同但题干含义不同的条目分别保留，并带有稳定题号冲突异常。（验证：同号异题夹具；AC70）
- [ ] 使用相同草稿和边界决策重复整理两次，条目数量、顺序、内容和异常完全一致。（验证：幂等测试；AC76、N45）
- [ ] 第 10、12 题重复复现场景从 17 条收敛为 15 条有效题，答案匹配不再出现这两题的重复题号冲突。（验证：脱敏真实夹具；AC71）

### 数据迁移与历史兼容

- [ ] v6 数据库升级后历史题目的重复状态均为有效，历史评分运行均为未过期，原数据数量和外键关系不变。（验证：v7 迁移测试和升级前后行数对比；AC77、N44、N49）
- [ ] 对已升级数据库重复执行迁移不会报错、重复字段、重复索引或改变业务数据。（验证：连续执行两次迁移；N45）
- [ ] 历史审核任务可正常打开，并可执行标记重复和恢复而无需重新上传文件。（验证：历史数据库 API 集成测试；AC77）

### 标记重复题

- [ ] 有效题操作栏显示“标记为重复”，点击后出现确认框，明确说明退出统计、释放答案和可恢复。（验证：Testing Library 交互测试；AC72、N51）
- [ ] 取消确认不会发送请求或改变当前题目、统计和答案占用。（验证：前端请求桩断言）
- [ ] 确认标记后题目数据库记录仍存在，但从普通导航、有效题总数、已确认数和完成校验中退出。（验证：API 调用前后数据库和页面对比；AC72、AC75）
- [ ] 被标记题目的 `answer_entry_id` 被释放，原答案立即出现在未使用答案区域，匹配行显示排除状态。（验证：审核详情与数据库查询；AC72）
- [ ] 标记已完成任务中的题目会使任务回到 `review_pending`，不能继续显示为已完成。（验证：任务状态集成测试）
- [ ] 对同一题连续标记两次不重复写审计、不重复失效下游，也不改变其他题目匹配。（验证：幂等 API 测试；AC76）

### 重复题查看与恢复

- [ ] “重复题”筛选只显示被标记题目，并以文字明确表示重复状态。（验证：UI 筛选测试；AC73、N51）
- [ ] 重复题内容保持可查看但编辑、区域修改、匹配修改、确认和重新打开操作均被前后端阻止。（验证：UI 禁用状态与绕过前端 API 测试）
- [ ] 点击恢复后题目返回普通导航并处于待确认，重复筛选数量同步减少。（验证：恢复交互测试；AC73、N50）
- [ ] 恢复不会直接复用旧匹配；只有当前未占用且满足安全规则的答案会形成新建议。（验证：唯一答案、已占用答案和同号冲突参数化测试；AC73）
- [ ] 恢复单题不会覆盖其他题目的教师匹配或造成一个答案被两题占用。（验证：匹配数据库唯一性和前后快照比较）
- [ ] 对同一题连续恢复两次不重复产生匹配、审计或下游失效副作用。（验证：恢复幂等测试；AC76）

### 并发保护与下游失效

- [ ] 学生答卷处于对齐或识别状态时，标记和恢复均返回 409 及可理解原因，题目状态不变。（验证：活跃提交集成测试；AC74）
- [ ] 评分运行处于排队、识别、评分、审计或生成物阶段时，标记和恢复均被阻止。（验证：各活跃评分状态参数化测试；F92）
- [ ] 非运行中学生提交在题目状态变化后退回需要重新处理状态，旧学生响应仍保留供追溯。（验证：数据库前后状态和行数对比；AC74、N44）
- [ ] 既有评分运行设置为过期但保留原完成状态、题级结果和证据；当前批注与错题报告变为 `stale`。（验证：生命周期测试及评分 API 查询；AC74）
- [ ] 评分工作台对过期运行显示明确文字提示，不把旧结果作为当前结论。（验证：评分 UI 测试）

### 审计与数据完整性

- [ ] 自动边界合并、合并降级、标记重复和恢复均生成包含任务、条目、动作和操作者的审计事件。（验证：查询 `audit_events`；AC75）
- [ ] 标记审计包含旧匹配摘要，恢复审计包含新建议状态，且不记录 API Key 或完整页面数据。（验证：审计载荷断言；AC75）
- [ ] 标记、恢复和重新匹配前后，原始 PDF、页面图像及历史模型响应摘要保持不变。（验证：SHA-256 与运行记录快照对比；AC75、N44）

### 前端一致性与可访问性

- [ ] 标记或恢复成功后，题目导航、有效题总数、已确认数、未使用答案数和完成按钮状态在一次查询刷新后一致。（验证：完整审核页交互测试；N50）
- [ ] mutation 进行中禁止重复提交和冲突导航，并显示处理中状态；失败时保留当前题目并显示服务端消息。（验证：延迟及失败请求桩；AC72）
- [ ] 确认框支持键盘进入、取消、确认和焦点返回；重复状态和危险操作不只依赖颜色。（验证：键盘 Testing Library 测试；N51）
- [ ] 1280×720 桌面视口下，重复题筛选、确认框、恢复按钮及底部审核操作均可滚动到达。（验证：浏览器页面观察）

### 编译、测试与静态检查

- [x] 边界、整理、匹配、迁移和生命周期单元测试全部通过。（验证：运行对应 `pytest` 文件）
- [x] 识别—匹配—标记—恢复后端集成测试全部通过。（验证：运行 `backend/tests/integration/test_api_workflow.py`）
- [x] 重复题与评分过期 UI 测试全部通过。（验证：运行对应 `vitest` 文件）
- [x] 完整 Python 和前端测试无回归。（验证：后端全量 pytest 与前端全量 Vitest 退出码为 0）
- [x] Ruff、Mypy、TypeScript 类型检查和 Python 编译检查通过。（验证：直接调用对应工具，退出码均为 0）
- [x] 生产构建成功，构建产物不包含 API Key、本机绝对路径或未经授权的远程资源。（验证：Vite build 及 `dist` 文本扫描）

### 端到端场景

- [ ] 场景 H：上传含跨页题目与跨页解析的试卷—答案组合 → 试卷和答案主批次均无重叠 → 每个边界只调用一次模型 → 跨页内容各合并为一个有效条目 → 答案正确匹配。（验证：完整本地任务与运行记录；AC78）
- [ ] 场景 I：边界模型返回不确定或调用失败 → 主识别结果保留并显示待审核异常 → 教师可将漏网重复题标记为重复 → 答案释放且任务统计一致。（验证：故障注入端到端流程；AC68、AC72、AC78）
- [ ] 场景 J：教师切换到重复题筛选并恢复题目 → 题目回到待确认 → 只生成安全匹配建议 → 旧学生评分和生成物明确过期 → 原始证据不变。（验证：审核与评分工作台完整流程；AC73-AC75、AC78）

### 增量验收报告

- [x] `docs/acceptance-report.md` 记录 AC63-AC78 的自动化通过证据、测试命令、模型调用计数和数据库行为摘要；真实模型与页面观察列为上线前抽验。（验证：逐条检查验收标准—证据映射）
- [ ] 所有未通过项均已修复并复验，或明确记录阻塞原因、影响范围和用户接受结论。（验证：检查报告未通过与复验章节）

## 增量 Checklist：多空填空题评分配置初始化

> 本节验证自动初始化预览，不允许通过新增模型调用补救；所有派生配置在教师保存前不得写入数据库。

### 空位数量与区域

- [x] 题干含三个连续下划线空位且只有一个复合答题区域时，返回三个连续空位 B1-B3，而不是一个空。（验证：初始化单元测试；AC79）
- [x] 单个 LaTeX 下划线或公式下标不会被统计为填空空位。（验证：题干标记对照测试；F94）
- [x] 题干没有明确空位但有三个独立答题区域时，返回三个空位。（验证：区域数量单元测试；F94）
- [x] 三个独立区域按页码、纵坐标和横坐标顺序分别分配给 B1-B3。（验证：乱序区域输入测试；AC81）
- [x] 一个复合区域用于三个空时，三个空均获得共享初始区域并产生明确共享提示。（验证：复合区域测试；AC79）
- [x] 题干空位数和区域数冲突时采用题干明确标记数量，并显示证据冲突提示。（验证：数量冲突测试；F94、F100）

### 参考答案安全拆分

- [x] “失去 异种 吸引”在预期三空时依次分配为 B1=失去、B2=异种、B3=吸引。（验证：真实示例回归；AC79）
- [x] “1×10⁻⁶ 负”在预期两空时保持科学计数法完整并分配为两个答案。（验证：科学计数法回归；AC80）
- [x] `(1)电荷转移 遵守 (2)CD` 在预期三空时移除组织序号并依次生成三个答案。（验证：编号组展平测试；F96）
- [x] 换行、中文分号和英文分号只有在恰好产生预期数量的非空片段时才被接受。（验证：强分隔符参数化测试；F96）
- [x] `2 m/s` 不会因预期两空而拆成数值和单位。（验证：数值单位保护测试；AC80）
- [x] 成对数学定界符、LaTeX 命令、括号表达式及运算符边界不会被普通空格拆坏。（验证：数学表达式参数化测试；N56）
- [x] 参考答案无法安全拆成预期数量时，各空标准答案保持为空，整条答案不写入 B1，并返回“答案分配需要检查”。（验证：歧义降级测试；AC82）

### 分值与初始化生命周期

- [x] 4 分三空生成 1.33、1.33、1.34，精确加总为 4.00。（验证：十进制分值测试；AC83）
- [x] 5 分两空生成 2.50、2.50；不同合法满分和空位数均保持总分守恒。（验证：分值参数化测试；AC83）
- [x] 未配置填空题的 GET 返回 source=derived、空位信号和警告，但数据库空位行数不变。（验证：API 前后数据库查询；AC87）
- [x] 相同未配置题连续查询两次，响应中的空位、顺序、答案、分值、区域和警告完全一致。（验证：幂等 API 测试；N53）
- [x] 教师保存派生配置后再次查询返回 source=saved，所有教师修改原样保留。（验证：GET—PUT—GET 集成测试；AC84）
- [x] 已有一个或多个保存空位时，修改题干、区域或匹配答案不会触发自动重建或覆盖。（验证：历史配置保护测试；AC84）
- [x] 单空填空题仍生成一个 B1，选择题和计算题返回 source=none 且空位为空。（验证：题型兼容测试；AC85）

### 前端显示与编辑

- [x] derived 三空响应在评分配置面板显示三个独立编辑行及各自答案和分值。（验证：Testing Library 组件测试；AC79）
- [x] derived 配置显示“自动初始化尚未保存”，保存成功后切换为已保存状态。（验证：保存交互测试；F100）
- [x] 歧义响应显示预期空位数和“答案分配需要检查”，原参考答案仍在题目答案区域可见。（验证：歧义 UI 测试；AC82）
- [x] 前端不再创建固定 B1，也不会在 API 已返回多个空时覆盖为空或单空。（验证：组件请求桩与源码搜索；F94）
- [x] 教师可通过键盘依次访问每空分值、答案类型、标准答案、同义答案和删除按钮。（验证：键盘交互测试；AC86、N57）
- [x] 教师增删空位后编号和 sortOrder 连续，保存请求包含正确顺序、逐空答案和总分。（验证：PUT 请求体断言；AC86）
- [x] 切换题目时上一题的派生警告、编辑内容和保存消息不会泄漏到下一题。（验证：组件 rerender 测试；N53）

### 集成、性能与无模型约束

- [x] 评分配置派生查询没有调用模型客户端、没有网络请求，也没有新增识别运行记录。（验证：模型桩调用计数和数据库查询；N54）
- [x] 初始化模块可以在无数据库、无 FastAPI 和无前端环境下独立运行单元测试。（验证：依赖导入检查；N53）
- [x] 现有保存请求的每空答案非空、键和顺序唯一、分值总和校验继续生效。（验证：原有及新增 API 校验测试；AC86）
- [x] 已保存的历史多空配置能够进入现有逐空评分输入，初始化改造不改变评分算法和结果。（验证：现有填空评分回归测试；N55）
- [x] 单题初始化只执行短文本与少量区域规则，不产生可感知等待。（验证：批量执行固定夹具并记录耗时；N58）

### 编译、测试与构建

- [x] 多空初始化单元测试全部通过。（验证：运行 `backend/tests/unit/test_blank_initialization.py`；AC88）
- [x] 评分配置 API 集成测试全部通过。（验证：运行 `backend/tests/integration/test_grading_api.py`；AC88）
- [x] 多空评分配置 UI 测试全部通过。（验证：运行 `tests/ui/grading-config.test.tsx`；AC88）
- [x] 完整后端与前端测试无回归。（验证：全量 pytest 与 Vitest 退出码为 0；AC88）
- [x] Ruff、Mypy、TypeScript 和 Python 编译检查通过。（验证：对应工具退出码均为 0；AC88）
- [x] Vite 生产构建成功，新增响应契约未导致运行时构建错误。（验证：生产构建退出码为 0；AC88）

### 端到端场景

- [ ] 场景 K：打开三个空但只有一个复合区域的填空题 → 看到三个正确答案和总分守恒的派生配置 → 教师保存 → 重新打开保持 saved 且内容不变。（验证：审核页完整交互；AC89）
- [ ] 场景 L：打开答案结构歧义的三空题 → 看到三个空和检查提示而非整条 B1 → 教师补齐答案并保存 → 配置通过现有校验并进入一次逐空评分。（验证：配置与评分组合流程；AC82、AC89）

### 增量验收报告

- [x] `docs/acceptance-report.md` 记录 AC79-AC89 的测试命令、通过数量、数据库无写入证据、页面观察和未执行项。（验证：逐条检查验收标准—证据映射）
- [x] 所有未通过项均已修复并复验，或明确记录阻塞原因、影响范围和用户接受结论。（验证：检查报告未通过与复验章节）

## 增量 Checklist：学生答卷自动批改、证据可见性与整页工作台

> 本节所有项目必须通过自动化测试、数据库查询、捕获日志或实际页面观察获得证据。不得用“预期如此”代替运行结果；涉及学生数据的日志和报告只保存脱敏标识与统计。

### 数据迁移与版本绑定

- [ ] v8 数据库升级到 v9 后，`grading_runs` 包含处理版本和触发来源，自动尝试表、外键、状态约束及索引完整存在。（验证：运行数据库迁移测试并查询 `PRAGMA table_info/index_list`；AC99）
- [ ] 历史评分运行升级后仍可读取，触发来源为兼容默认值，旧输入快照、题级结果、证据和生成物行数不变。（验证：升级前后行数、外键和 API 响应对比；N71）
- [ ] 同一学生处理版本不能保存两个自动评分运行，并发冲突被应用转换为复用已有运行而不是 500。（验证：并发插入及协调器集成测试；AC99）
- [ ] 新学生处理版本能够创建新的自动运行，旧运行和生成物保留历史并明确过期。（验证：版本切换生命周期测试；AC99、N71）
- [x] 对已升级数据库重复执行迁移不会增加表、索引、尝试或业务数据，也不会改变历史运行内容。（验证：连续执行两次迁移并比较 schema 与行数）

### 统一日志与请求关联

- [ ] 应用启动后控制台日志和配置启用的文件日志均为可解析 JSON Lines，包含时间、级别、logger 和事件名。（验证：启动测试应用，逐行 JSON 解析日志；F119）
- [ ] `X-Request-ID` 格式合法时被响应回传，非法或超长值被替换；同一请求的完成或错误日志使用相同标识。（验证：HTTP 中间件集成测试；F119、N65）
- [ ] HTTP 日志只包含方法、路由模板、状态和耗时，不包含查询字符串、请求头、表单、文件名或请求体。（验证：带哨兵参数的请求及捕获日志扫描；N66）
- [ ] 学生处理日志按开始、页面准备、配准、题框映射、识别和提交结果排序，并可由答卷与处理版本标识串联。（验证：学生流水线日志断言；AC100、N65）
- [ ] 自动评分日志包含运行创建、预检、逐题汇总、审计、生成物、待复核/完成或失败，并可与同一上传链路关联。（验证：完整自动流程日志事件序列；AC100、AC106）
- [ ] 模型调用日志只记录阶段、模型、尝试、状态、耗时和用量，不记录提示词、图片编码、学生答案或原始响应。（验证：模型桩与哨兵内容扫描；N66）
- [ ] 后台任务未捕获异常与生成物失败包含完整堆栈，不出现 `Task exception was never retrieved`，取消任务不被记录为服务故障。（验证：异常、取消和关闭任务管理器测试；AC100）
- [ ] 达到 `LOG_MAX_BYTES` 后生成的日志备份数量不超过 `LOG_BACKUP_COUNT`，最近诊断事件仍可读取。（验证：小阈值轮转单元测试；F120）
- [ ] 捕获日志、当前日志和所有轮转文件中搜索不到测试 API Key、Authorization、Cookie、学生姓名、学号、识别全文、标准答案、提示词、模型原始响应或图像内容哨兵。（验证：完整目录文本扫描；AC106、N66）

### 上传后自动串联批改

- [ ] 上传评分配置就绪且页面可靠的学生答卷后，接口返回 202，后台无需教师审核或手动点击即可依次完成学生处理与评分。（验证：上传—等待—数据库/API 集成测试；AC97）
- [ ] 自动运行绑定上传产生的当前处理版本，`triggerSource=automatic`，输入快照中的版本与顶层字段一致。（验证：查询运行详情和数据库；AC97、AC99）
- [ ] 页面配准、题框映射和有效题区域全部可靠时允许自动评分；原始题目、标准答案、题框版本或评分配置未满足既有门禁时仍阻断。（验证：安全门禁参数化测试；AC97、N64）
- [ ] 配准失败、题框缺失、学生页面缺失或处理版本不一致时不创建评分结果，并显示稳定代码和修正建议。（验证：异常答卷夹具及列表/API 状态；AC97、F110）
- [x] 低置信度或不可辨认但证据区域有效的学生作答不会阻止其他题评分，本题生成结果并进入批改后的复核队列。（验证：混合置信度整卷测试；AC98）
- [x] `recognition_needs_review` 只作为题级风险进入评分；`mapping_needs_review` 仍为几何阻断，两者不会被混淆。（验证：处理状态参数化测试；AC98、N64）
- [ ] 同一处理版本重复触发、并发触发、上传请求重试和模拟应用重启补触发均只保留一条自动尝试和一个运行。（验证：协调器幂等与重启测试；AC99）
- [ ] 旧版本工作流晚完成时不能覆盖新处理版本的自动尝试、运行或列表状态。（验证：受控并发时序测试；AC99）
- [ ] 已完成但没有自动尝试的历史提交可以通过兼容补启动入口生成一次绑定当前版本的运行。（验证：历史提交 API 集成测试；N71）

### 失败状态与恢复

- [ ] 评分运行创建前因配置或答案门禁失败时，自动尝试保存 `blocked`、稳定错误代码和可理解消息，学生列表不只显示“处理中”。（验证：预检故障注入；AC100）
- [ ] 逐题评分或生成物阶段失败时，评分运行和自动尝试都显示失败阶段、最后成功阶段、已完成题数和是否可重试。（验证：评分及生成物故障注入；AC100、F112）
- [ ] 可重试失败从持久化检查点继续，已完成题目不重复调用模型；恢复后的结果与不中断运行一致。（验证：中断恢复和调用计数测试；AC100）
- [ ] 不可重试失败引导教师修正评分配置、标准答案或配准问题，不提供会重复失败的误导性按钮。（验证：学生列表和工作台 UI 测试；F112）
- [ ] 自动工作流取消时学生处理或评分写入明确中断状态，临时任务被清理，不留下永远 running 的尝试。（验证：取消集成测试）

### 识别风险与证据完整性

- [ ] 题级结果没有任何证据时，无论该题原始得分为满分、零分或部分分，均产生且只产生一个 `MISSING_EVIDENCE` 复核原因。（验证：评分审计参数化测试；AC98、F114）
- [ ] 有效证据题不会因新增门禁产生错误的 `MISSING_EVIDENCE`，现有得分项证据和错误位置检查继续生效。（验证：现有评分审计回归；F114）
- [ ] 每个当前最终题目结果至少包含页、区域、原图坐标和识别文本；判分详情包含标准答案、规则/工具、原因、得分及引用证据。（验证：查询最新 15 题详情并检查结构；AC102）
- [ ] 低置信度题的复核原因、识别问题代码和证据都保留在题级结果中，教师复核后历史工具观察仍可追溯。（验证：复核前后 API 与数据库快照；AC98）
- [ ] 证据坐标使用评分运行绑定的学生处理版本和学生原图空间，不使用当前新版题框替代历史运行事实。（验证：创建新题框版本后重新查询旧运行；N67）

### 证据裁剪 API 安全

- [ ] 正常证据预览返回 JPEG，裁剪尺寸与保存证据框一致，并带私有缓存头和稳定 ETag。（验证：证据 API 图像尺寸和响应头测试；AC102、AC103）
- [ ] 请求其他题结果的区域、其他学生提交的页面或不存在的区域均被拒绝，不返回任何图像字节。（验证：跨结果和跨提交攻击测试；AC103、N67）
- [ ] NaN、Infinity、零/负尺寸和越界坐标均返回稳定错误，不自动扩框、猜测或钳制成另一区域。（验证：坐标校验参数化测试；AC103）
- [ ] 学生原图缺失或损坏时返回安全错误，不暴露数据目录或操作系统绝对路径。（验证：文件故障测试与响应文本扫描；AC103）
- [ ] 客户端无法通过路径、查询参数或伪造坐标读取数据目录外文件。（验证：路径穿越哨兵文件测试；AC103、N67）
- [ ] 获取证据详情和预览不会修改数据库证据快照、运行版本或生成物状态。（验证：请求前后 JSON、修订号和行数对比）

### 批改证据前端展示

- [x] 打开当前批改工作台时“识别证据”默认勾选，当前页有效证据框无需额外操作即可看到。（验证：Testing Library 复选框与 SVG 断言；AC102）
- [x] 右侧存在明确标题“批改证据”，显示学生识别、标准答案、评分规则/工具、判分原因、得分、页码和证据预览。（验证：证据面板组件测试；AC102）
- [ ] 每个证据卡片能够说明它引用的具体判分项；未被分项显式引用的证据标为题级证据。（验证：多判分项证据夹具；F115）
- [ ] 点击或键盘激活证据卡片会切换到其页面并使用明显轮廓高亮对应区域，当前题和判分详情不改变。（验证：跨页证据 UI 测试；AC102、AC105）
- [ ] 证据图片加载失败时识别文字、规则、原因和得分仍保留，并显示图片失败说明。（验证：图片 error 事件组件测试；AC103）
- [ ] 历史运行没有预览地址时显示兼容说明而不崩溃；完全没有证据时显示 `MISSING_EVIDENCE` 与重新处理/复核建议。（验证：历史与空证据组件测试；AC103、N71）
- [ ] 证据状态、当前选中和错误均有文字或语义，不只依赖蓝色、虚线和图标。（验证：角色、可访问名称及无样式 DOM 检查；N69）

### 持久批改进度

- [ ] 排队、预检、逐题批改、审计、生成批注和生成报告分别显示正确阶段文字和非 100% 进度。（验证：进度纯函数及组件参数化测试；AC101）
- [ ] 逐题批改显示已完成题数、总题数和百分比，题数为零或异常越界时仍返回安全范围。（验证：进度边界测试；AC101、N68）
- [ ] 同一正常运行从开始到完成的显示百分比只增加或保持，不在刷新或阶段切换时倒退。（验证：阶段序列测试；N68）
- [ ] 待复核状态保留进度条并显示“自动批改完成，N 项待复核”；完成状态保留 100% 和最终文字。（验证：终态 UI 测试；AC101）
- [ ] 失败状态保留最后可信阶段和题数，不显示为 100% 完成；重试后从持久进度继续。（验证：失败—重试组件与集成测试；AC101、N68）
- [x] 进度组件具有 `role=progressbar`、当前值、最小值、最大值和可读名称，状态不只依赖颜色。（验证：Testing Library 可访问性断言；N69）
- [ ] 学生列表在学生处理完成后继续轮询自动运行，显示批改阶段、小型进度、得分或待复核数。（验证：学生列表轮询和状态测试；AC109）

### 整页适配、缩放与布局

- [x] 1697×2400 页面在整页模式下按查看区宽高较小比例缩放，计算画布宽高均不超过可用空间。（验证：视口纯函数测试；AC104）
- [x] 宽度适配以可用宽度为基准并允许明确的中央纵向滚动；实际大小使用 1 图像像素对应 1 CSS 像素。（验证：三模式计算测试；F116）
- [ ] 缩放被限制在 25%–300%，放大、缩小、100%、整页和宽度按钮显示当前模式与比例，并在边界正确禁用。（验证：工具栏交互测试；AC104）
- [ ] 在 1366×768、1440×900 和 1920×1080 中，默认整页模式无需上下滑动画布即可看到完整单页。（验证：浏览器记录容器和 SVG 边界；AC104、N70）
- [ ] 整页模式的页面主体和中央查看区不会同时出现竞争的纵向滚动条；右侧详情保持独立滚动。（验证：浏览器 `scrollHeight/clientHeight` 与页面观察；N70）
- [ ] 聚焦模式折叠两侧栏并扩大试卷区域，退出后当前题、页码、缩放、图层和当前证据保持不变。（验证：聚焦模式 UI 测试；AC104、F118）
- [ ] 小于 1200px 时使用“题目 / 试卷 / 证据与判分”面板切换，不把三栏压缩成不可读宽度。（验证：响应式浏览器与组件测试；F118）
- [ ] 切换题目时优先跳到首个证据页，无证据时跳到捕获题框页；跨页选择不会停留在无关页面。（验证：跨页题目 UI 测试；AC105）
- [ ] 普通鼠标滚轮和触控板连续滚动不会切换页码或改变缩放；翻页只由明确按钮或键盘快捷键触发。（验证：wheel 事件测试与实际触控板观察；AC105、N70）
- [ ] 错误位置绘制期间禁用浏览平移和触控滚动，拖框仍保存原图像素坐标而非缩放后 CSS 坐标。（验证：pointer 事件与坐标换算测试；AC105）
- [ ] 所有缩放、聚焦、翻页、侧栏和面板切换按钮可由键盘访问并具有明确名称。（验证：键盘交互与可访问性测试；N69）

### 学生列表与恢复入口

- [ ] 上传按钮明确显示“上传并自动批改”，上传成功反馈说明后台将继续批改。（验证：学生页 UI 测试；F109）
- [ ] 自动运行存在时提供“查看批改进度/结果”，进入后选中该运行而不是创建重复手动运行。（验证：导航请求桩与运行列表测试；AC109）
- [ ] 自动尝试阻断时列表显示错误代码、原因及对应配置/配准/重新处理入口。（验证：阻断状态组件测试；AC100）
- [ ] 历史提交没有自动尝试时仍可进入兼容手动批改，不因新增字段为空导致按钮永久禁用。（验证：历史响应 UI 测试；N71）
- [ ] 人工配准修正后继续识别会通过组合工作流自动补启动新版本评分，无需再次点击开始。（验证：人工配准—识别—自动评分集成测试；AC97）

### 必要注释与架构约束

- [x] 自动门禁代码解释“识别风险后置、几何风险前置”的原因，不只是复述状态名称。（验证：审查协调器和评分输入分支；AC107）
- [x] 数据库幂等和旧版本条件更新旁说明内存任务锁不能替代唯一约束及为何要绑定处理版本。（验证：审查迁移和协调器；AC107）
- [x] 证据 API 说明不能信任客户端路径/坐标以及证据页归属校验顺序。（验证：审查证据模块文档字符串；AC107）
- [x] 进度权重和整页缩放公式旁说明“不提前 100%”与同时受宽高约束的原因。（验证：审查进度和视口纯函数；AC107）
- [x] SVG 原图坐标与 CSS 显示尺寸分离、绘制换算和当前证据高亮具有必要注释。（验证：审查覆盖层；AC107）
- [x] 本轮修改中没有逐行复述赋值/样式的噪声注释，也没有遗留 TODO、TBD 或与实现不一致的说明。（验证：代码审查与文本搜索；AC107、N72）

### 编译、测试与静态检查

- [x] v9 迁移、日志、自动工作流、评分风险和证据 API 单元测试全部通过。（验证：运行对应后端测试文件；AC108）
- [ ] 上传后自动批改、低置信度后置复核、并发幂等和证据 API 集成测试全部通过。（验证：运行新增 integration 测试；AC108）
- [ ] 批改进度、证据面板、视口计算、工作台和学生列表 UI 测试全部通过。（验证：运行新增及修改的 Vitest 文件；AC108）
- [x] 完整后端 pytest 与完整前端 Vitest 无回归。（验证：`pnpm test` 退出码为 0；AC108）
- [x] Ruff、Mypy 和 TypeScript 类型检查全部通过。（验证：`pnpm lint` 退出码为 0；AC108）
- [x] Python 编译和 Vite 生产构建成功。（验证：`pnpm build` 退出码为 0；AC108）
- [ ] 生产构建和日志目录中搜索不到 API Key、本机敏感绝对路径或未授权远程资源。（验证：构建产物及日志文本扫描；N66）

### 端到端场景

- [ ] 场景 M：上传可靠多页学生答卷 → 留在学生列表观察学生处理和自动评分进度 → 不进行教师审核、不点击开始批改 → 自动产生题级结果、批注试卷和错题报告。（验证：真实或可控模型完整流程；AC97、AC109）
- [ ] 场景 N：答卷含一题低置信度识别 → 其他题正常完成 → 风险题出现在批改后复核队列且证据可见 → 教师确认后重新生成当前批注和报告。（验证：混合风险端到端流程；AC98、AC109）
- [ ] 场景 O：选择每道题 → 工作台自动定位首证据页 → 右侧显示识别、标准答案、规则、原因、得分和裁剪 → 点击跨页证据高亮准确区域。（验证：最新 15 题逐题页面检查；AC102）
- [ ] 场景 P：在三种桌面视口默认打开同一答卷 → 完整单页无需上下滑动 → 使用显式按钮缩放、聚焦和翻页 → 连续滚轮/触控板不误翻页或缩放。（验证：桌面浏览器实际操作；AC104、AC105）
- [ ] 场景 Q：自动评分在生成物阶段失败 → 列表和工作台显示失败阶段与最后进度 → 日志可按运行标识找到堆栈 → 重试后从检查点完成且不重复评分。（验证：故障注入与日志、调用计数；AC100、AC101）
- [ ] 场景 R：重新处理同一学生答卷生成新版本 → 自动创建新运行 → 旧运行、证据和文件保留但过期 → 新工作台只把新运行作为当前结论。（验证：版本生命周期完整流程；AC99、N71）

### 增量验收报告

- [x] `docs/acceptance-report.md` 记录 AC97-AC109 的实际测试命令、退出码、通过数量、数据库迁移与运行计数、日志脱敏扫描和页面观察。（验证：逐条检查验收标准—证据映射）
- [x] 报告包含 1366×768、1440×900、1920×1080 的整页尺寸记录，以及证据卡片、跨页高亮、持久进度和聚焦模式截图或等价可验证输出。（验证：审阅验收附件）
- [x] 报告明确区分自动化通过、真实浏览器通过和因硬件/模型环境未执行的项目，不把未执行写成通过。（验证：检查结果状态与原始证据）
- [x] 所有未通过项目均已修复并重新验证，或明确记录阻塞原因、影响范围和用户接受结论。（验证：检查报告未通过与复验章节）
